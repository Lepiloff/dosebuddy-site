# v1.1 — контракт API (Часть 5)

Шов между двумя треками. Всё остальное каждый трек решает у себя; здесь оба
обязаны совпасть.

Основание — `dosebuddy/docs/05-technical-spec-v1.1.md` (Часть 5) и решения в
[`v1.1-contract-decisions.md`](v1.1-contract-decisions.md).

## Статус документа

| Раздел | Статус |
|---|---|
| Формат, ошибки, версионирование | предложение backend-трека |
| Auth, devices, pairing | предложение backend-трека |
| Sync: курсор, конфликты, проекция по роли | предложение backend-трека |
| Алерты | согласовано с владельцем 2026-08-10 |
| Поля сущностей и значения enum | **взяты из существующего кода приложения**, не придуманы |
| Новые локальные таблицы (§7) | **принято app-треком целиком** (`05d` §4) |

Редакция 21 от 2026-09-17 — **`account_id` получателя в обоих push-payload'ах**
(§4.3, §5). Задержанный push, выданный одному аккаунту, приходил на телефон,
вошедший с тех пор в другой, и отличить его было нечем; у nudge это хуже, чем у
алерта, потому что он велит замолчать. Поле строковое (тип FCM), старые клиенты
его игнорируют, серверная половина обязана выйти раньше клиентского guard'а.
Форма согласована с клиентским треком 17.09. **P0 «готовность» этой редакцией не
закрывается** — его протокол спроектирован в §4.3 и не реализован.
Редакция 20 от 2026-09-17 — **два новых endpoint'а по контракту клиентского
ревью** (§1, §4.3). `GET /sync/preview` — та же лента, что `/sync/pull`, но
`Device.cursor_seq` не двигается: чтение вперёд ради выбора профиля больше не
считается выдачей и не отпускает шлюз передачи авторитета. `POST
/account/delete-retry` завершает удаление, ответ на которое потерялся, по
исходному `account_id` с проверкой Google-токена: `204` если аккаунта нет или он
удалён, `403` если чужой, `401` на плохой токен, `429` при частоте; `/auth/google`
не зовётся и новый аккаунт не создаётся. `/sync/pull` со старыми клиентами
совместим. **Открытым** оставлен вопрос, что `cursor_seq` доказывает: сейчас
«страница выдана», а не «страница применена» — строгая форма описана в §4.3 и
ждёт подтверждения от клиентского трека.
Редакция 19 от 2026-09-09 — **неизвестный родитель — это `retry`, а не отказ**
(§4.2, §10.7). Профильный край отвечал `forbidden_role` и на «профиля нет
вовсе», и на «профиль чужой»; первое окончательным быть не должно. App-трек
замерил, чего это стоит: их исходящий журнал может отправить лекарство раньше
его профиля, окончательный отказ уходит в карантин, карантин не переигрывается,
нового `server_seq` отвергнутая строка не получает — и лекарство остаётся жить
на одном телефоне. Теперь `medications.profile_id` и `dose_events.profile_id`
отвечают `missing_parent`, если профиля нет, и по-прежнему `forbidden_role`,
если он есть и чужой — то есть так же, как рёбра лекарства вели себя всегда.
Клиентских изменений не требует **логически** — ветвлений по коду отказа у
клиента нет вовсе. Но до клиентского фикса порядка батча (§10 п. 7) она меняет
невосстановимую потерю строк на восстановимый останов отправки, и что из этого
хуже — решение владельца. Написана, протестирована, не выложена; обе стороны
счёта — в §10 п. 7 и в `docs/debts.md`.
Редакция 18 от 2026-09-09 — **у дозы родителей два** (§4.2, §4.4, §10.6).
Ред. 17 закрыла `dose_events.profile_id` и оставила `dose_events.medication_id`
открытым — то есть закрыла ту половину строки, которую никто не атаковал.
Найдено ревью app-трека, воспроизведено на сервере: один аккаунт, без гонки,
доза переезжает на другое лекарство, и на телефоне это тише всего остального —
авторитет напоминания читается из `profile_id`, звонок остаётся правильным, а
текст и списание запаса берутся из `medication_id`. Теперь `immutable_parent`.
Записано и то, чего документ раньше не говорил: **отвергнутая строка не получает
нового `server_seq`, поэтому `pull` её не пришлёт** — карантин сам не
рассасывается. Клиентских изменений требует ровно одно: карантин распространить
на шестую связь.
Редакция 17 от 2026-09-09 — **то же правило для остальных четырёх связей**
(§4.2, §4.4, §10.6). Проверка, которую исправила ред. 16, стояла ещё в четырёх
местах: `push` авторизовал запись по родителю, которого она **называет**, а не
по тому, который у лежащей строки уже есть. Все четыре воспроизведены на
настоящей базе. Самая тяжёлая — `profiles.owner_account_id`: опекун с
**отозванным** доступом профиль уже не видит, но идентификатор помнит, и пуш
строки профиля переписывал владельца на него — вместе с проекцией владельца,
включая расписания, которых наблюдателю не выдают. Самая доступная —
`dose_events.profile_id`: идентификаторы доз наблюдателю выдаются по построению.
Плюс `schedules.medication_id`, `stock_events.medication_id` и проверка
`dose_events.medication_id`, которой не было вовсе. Коды: `immutable_parent`
везде, кроме профиля, где `forbidden_role`. **Клиентских изменений требует** —
пункт 6 в §10 расширен с одной сущности на все.
Редакция 16 от 2026-09-09 — **`medications.profile_id` неизменяем после
создания** (§4.2, §4.4). Смена родителя у существующего лекарства —
`rejected` с кодом `immutable_parent`; строка отвергается целиком, ни одно её
поле не применяется, `server_seq` не двигается. Запрет стоит в схеме (триггер,
миграция 0014), а не в обработчике: отказ поэтому атомарен, и правило держится
для любого писателя. Закрывает P0 app-трека — доза остаётся с прежним
`profile_id`, будильник отбрасывается как устаревший, телефон молчит — и заодно
возможность опекуна увести чужое лекарство в свой профиль. **Клиентских
изменений требует:** отвергнутую строку класть в карантин, а не считать
применённой. Переноса лекарства нет в UI, так что правило не отнимает ни одной
существующей функции. Принято ревьюером кода app-трека; владелец подтвердил
объём работ.
Редакция 15 от 2026-08-28 — исправлен базовый URL в §0: было
`https://api.dosebuddyapp.com/v1`, стало `https://api.sonadose.com/v1`. Хост в
документе был мёртвым с 2026-08-21, когда `api.dosebuddyapp.com` сняли вместе с
доменом. App-трек перевёл `apiBaseUrl` раньше, чем это доехало сюда, так что
расхождение жило только в тексте и ни на одну сборку не повлияло. Контракт этим
не меняется — исправляется факт о том, где стоит сервер. Клиентских изменений
не требует.
Редакция 14 от 2026-08-21 — по внешнему ревью v1.1 (Codex, разобрано
app-треком). Три исправления на стыке. `POST /profiles/{id}/reminder-authority`
отвечает **`200` с телом** `{owner_device_id, revision}` вместо `204` — номер
сервер вычислял и раньше, но отдавал только проигравшему устройству, из-за чего
забор заявителя от опоздавшего nudge оставался открытым (§3). Ветка «уже
владелец» отвечает тем же телом. Зафиксированы два свойства, на которые
опирается клиентская починка ownership: передача авторитета **не трогает**
`updated_at`, и строка профиля возвращается в ленту `pull` при каждой передаче
(§4.3). И исправлено давнее враньё в §4.2: размерный лимит — это `413`, а не
`429`; `429` приходит от nginx и означает частоту, а не размер. Теперь он несёт
`Retry-After` и обычный конверт ошибки, у `/v1/sync/` появилось собственное
ведро частоты (§0), а сами лимиты сведены в одно место: они стояли и в §4.2, и в
§4.3, разными числами, и клиент реализовал неверную половину. Клиентских изменений не требует; сборка,
лежащая в ревью Play, от `200` с телом только выигрывает.
Редакция 13 от 2026-08-18 — nudge проигравшему **откладывается**, пока курсор
нового владельца не пройдёт `server_seq` профиля (§3). Новый столбец
`devices.cursor_seq`. Причина — замер: мгновенный nudge давал 2 мин 36 с тишины
там, где медленный давал 28 с дубля, а тишина по инварианту №1 хуже. Клиентских
изменений не требует. Решение владельца 2026-08-18.
Редакция 12 от 2026-08-18 — nudge о потере авторитета отправляет **воркер**, а
не API: строка в `alert_deliveries` вида `reminder_authority_lost` с новым
столбцом `device_id` (§3). Тело собирается в момент отправки, поэтому ретрай не
может рассказать устаревшую правду. У API отправителя push больше нет вовсе.
Редакция 11 от 2026-08-18 — тело push `reminder_authority_lost` получило
`owner_device_id`, `revision` и `expires_at`, а строка профиля на `pull` —
`revision` (§3, §4.3). До этой редакции защита от протухшего nudge на устройстве
существовала, но читала поля, которых сервер не отправлял, и сравнивала ревизию,
которой не было на проводе. Все значения в `data` — строки, это тип FCM, а не
выбор. Наблюдателю `revision` не выдаётся.
Редакция 10 от 2026-08-15 — `POST /devices/{id}/sync-token`: долгоживущий
пропуск для фоновой синхронизации, область только sync (§2). Закрывает `05i`.
Редакция 9 от 2026-08-13 — TTL передаётся в FCM по типу сигнала (§5), уточнён
fallback-текст: он обязан работать и без имени профиля, не только без дозы
(§5). Редакция 8 — что `sent` означает и чего не означает, рецепт вызова
настоящего алерта для E2E, и три клиентские ловушки: push может обогнать данные,
текста в payload нет по построению, data-only не доходит до force-stop'нутого
приложения (§5). Редакция 7 — по второму кругу ревью: `profile_stale` считает молчание
только напоминающего устройства, `profile_id` вошёл в ключ уникальности и в
`collapse_key` (§5). Редакция 6 — по внешнему ревью и запросу app-трека: карта `roles`
в каждом ответе `pull` (§4.3), требования к схеме наблюдателя
(§4.3), доставка алертов at-least-once со схлопыванием и TTL (§5), пороги
`profile_stale` и максимальный возраст `dose_missed` (§5), исправлено имя поля в
push — `subject_id`, а не `dose_event_id`. Исчезновение профиля из `roles` названо сигналом отзыва
согласия (§4.3). Четыре блокирующих пункта к app-треку в §10. Редакция 5 от 2026-08-12 — личность операции (op_seq), три исхода push,
причинный порядок оговорён явно. Редакция 4 от 2026-08-11 — добавлена передача авторитета напоминаний,
исправлена форма ответа `/auth/google`. Редакция 3 от 2026-08-10 — учтён ответ app-трека
`dosebuddy/docs/05d-api-contract-response.md` и его «Ред. 2». Что изменилось и
почему — §8 и §9.

Поля `profiles`, `medications`, `schedules`, `dose_events`, `stock_events` и
значения enum прочитаны из `lib/core/db/tables.dart` и `enums.dart`. Сервер их
повторяет, а не переопределяет: устройство — источник истины (§0.5).

**О терминологии.** Везде «профиль» и «устройство профиля», а не «мама» и не
«родитель». Роль живёт на связке (аккаунт ↔ профиль) (§1.2), у одного аккаунта
могут быть одновременно свои и наблюдаемые профили (§1.3, случай 2), и профиль —
не обязательно пожилой родственник. Формулировки, предполагающие обратное,
однажды окажутся ложью в интерфейсе.

---

## 0. Общие правила

**База:** `https://api.sonadose.com/v1`

**Версия в пути.** Клиент в проде и сервер эволюционируют независимо, сервер
держит совместимость с уже опубликованными сборками (§5.3). `/v2` появляется
только при несовместимом изменении; добавление необязательного поля таковым не
является.

**Авторизация:** `Authorization: Bearer <access_token>` на всём, кроме
`/auth/google` и `/auth/refresh`.

**Ошибки** — всегда один конверт:

```json
{ "error": { "code": "profile_not_found", "message": "…" } }
```

`code` — стабильная строка, на неё можно ветвиться. `message` — для логов
разработчика, не для показа пользователю: он не локализован и может измениться.
Внутренности наружу не попадают никогда (ни SQL, ни строки подключения, ни
трассировки) — см. тест `test_failure_detail_never_leaks_the_connection_string`.

Коды HTTP: `400` невалидный запрос, `401` нет/просрочен токен, `403` роль не
позволяет, `404` нет объекта либо нет доступа (различать эти два случая наружу
нельзя — иначе перебором узнаётся существование чужих профилей), `409`
конфликт состояния, `429` лимит, `503` не готов.

**Ограничение частоты.** Два ведра на адрес, оба живут в nginx, оба отвечают
`429`:

| Путь | Норма | Всплеск |
|---|---|---|
| `/v1/sync/…` | 30 запросов/с | 60 |
| всё остальное | 10 запросов/с | 20 |

`429` несёт `Retry-After: 5` и обычный конверт ошибки с кодом `rate_limited` —
значит ветвление по `error.code` не нуждается в отдельном случае для отказов, не
дошедших до приложения. Единственный `429` из самого приложения — перебор кода
пары (§3.2), и он про попытки, а не про частоту запросов.

**Ключ — IP-адрес, и это важнее, чем кажется.** Одиночный телефон практически не
способен упереться даже в 10 r/s: запросы последовательны, а отправка 200
записей по мобильной сети не укладывается в 100 мс. Лимит, стало быть, никогда
не был про одно устройство — он про адрес, а за одним адресом оператора (CGNAT)
стоит сколько угодно телефонов. Первая синхронизация давнего пользователя —
тысячи доз по 200 в пачке, десятки запросов подряд, и это штатное поведение, а
не злоупотребление. Ради него у `/v1/sync/` с ред. 14 своё ведро: до неё он
делил ведро с лендингом.

Отсюда правило для клиента: **никаких искусственных пауз между пачками**. Пауза
замедлила бы первую синхронизацию всем, чтобы защититься от того, чего один
пользователь вызвать не может. На `429` — ждать ровно столько, сколько сказано в
`Retry-After`, и не трогать размер батча (§4.2).

**Идемпотентность.** Все идентификаторы — UUID, сгенерированные на устройстве
(`SyncColumns.id`). Поэтому повтор `push` после обрыва связи безопасен: это
upsert по тому же `id`, а не второе создание.

**Время** — целое, миллисекунды UTC, как в модели устройства (`plannedAt`,
`updatedAt`). Ни строк ISO, ни локальных зон на проводе.

Исключение — поля расписания, которые по своей природе локальные и уже так
хранятся на устройстве; сервер повторяет их формат дословно, не разбирая:

| Поле | Формат |
|---|---|
| `schedules.times` | JSON-массив локального стенного времени, `["09:00","21:00"]` |
| `schedules.days_of_week` | JSON-массив номеров дней ISO, 1 = понедельник |
| `schedules.start_date`, `end_date` | ISO-8601 дата, локальная; `end_date` включительно |

Сервер их **не интерпретирует**: расписания разворачивает в дозы устройство
(§3.4), и второй реализации календарной логики на сервере быть не должно —
разойдутся.

---

## 1. Auth

Google Sign-In. Сервер проверяет Google ID token и выдаёт **свои** токены;
Google не становится источником сессии.

### `POST /auth/google`

```json
{ "id_token": "<google id token>",
  "device": { "id": "<uuid>", "platform": "android", "app_version": "1.1.0" } }
```

Ответ `200`:

```json
{ "access_token": "<jwt>", "expires_in": 900,
  "refresh_token": "<opaque>",
  "account_id": "<uuid>" }
```

> Ред. 4 исправляет ошибку документа: здесь стоял вложенный
> `account: { id, created_at }`, а сервер всегда отдавал плоский `account_id`.
> Прав код. `created_at` аккаунта клиенту не нужен и не отдаётся.

- Личность — Google `sub`. Email хранится (шифрованным) для поддержки, но
  **ключом не является**: email меняется, `sub` нет.
- `device.id` генерирует клиент и хранит постоянно. Смена `device.id` — это
  новое устройство со своей сессией, а не переустановка.
- Первый вход создаёт аккаунт. Отдельного «регистрации» нет.

### `POST /auth/refresh`

```json
{ "refresh_token": "<opaque>" }
```

- Refresh-токен **непрозрачный, хранится в БД хешем и ротируется при каждом
  использовании**. Самодостаточный JWT здесь не годится: «убрать опекуна» и
  «отвязать устройство» обязаны действовать немедленно, а отозвать JWT нельзя.
- Повторное использование уже ротированного токена трактуется как компрометация:
  вся цепочка этого устройства отзывается, ответ `401`.

### `POST /auth/logout`

Отзывает refresh-токен текущего устройства. Access доживает свои минуты — это
осознанно, иначе на каждый запрос нужна проверка в БД.

### `DELETE /account`

Удаление аккаунта: данные аккаунта, его устройств, членств и профилей, которыми
он владеет. **Обязательство Google Play**, появившееся вместе с аккаунтами;
нужен ещё публичный URL для тех, кто уже удалил приложение.

Профили, которые аккаунт только наблюдал, не трогаются — они не его.

### `POST /account/delete-retry`

```json
{ "account_id": "…", "id_token": "…" }
```

Завершает удаление, ответ на которое **потерялся**. Сессии не требует и её не
использует: к этому моменту её может не быть — ровно поэтому endpoint и нужен.

`DELETE /account` идемпотентен в том смысле, в каком это было нужно, когда его
писали: пришлите дважды — строка исчезнет один раз. Он не идемпотентен в том
смысле, который нужен клиенту, потому что второй заход делает **сессия**, а
сессии уже нет: аккаунта нет → refresh не проходит → телефон входит заново →
`/auth/google` создаёт **новый** аккаунт. Прежний повтор удалял его — строку,
прожившую десять секунд, — а та, ради которой всё затевалось, оставалась.
Клиент теперь сверяет id перед каждым DELETE и при несовпадении останавливается:
правильно и безвыходно.

| исход | ответ | что это значит |
|---|---|---|
| аккаунта уже нет | `204` | операция, которую повторяли, состоялась |
| аккаунт есть, `google_sub` совпал | `204` | удалён тем же транзакционным путём, что `DELETE /account` |
| аккаунт есть, subject другой | `403` `not_your_account` | **не удалён** |
| токен не проверился | `401` `invalid_google_token` | |
| слишком часто | `429` `too_many_attempts` | 10 попыток в час на Google subject |

`/auth/google` отсюда не вызывается и аккаунт не создаётся ни при каком исходе:
создать его здесь значило бы вернуть ровно ту строку, из-за которой возникла
путаница.

**Про `403` и информацию.** Он сообщает вызывающему с живой Google-личностью,
что некоторый uuid — чужой аккаунт; `/pairing/redeem` о кодах молчит и об этом.
Разница в том, что код — шесть символов, а тут v4 uuid: перебирать нечего,
вопрос всегда про id, который у вызывающего уже есть. Альтернатива — отвечать
`204` на чужой аккаунт — завершила бы клиентский цикл словом «удалено», пока
аккаунт стоит, и это единственный ответ здесь, по которому можно ошибочно
действовать. Частота всё равно ограничена: ограничение, которое важно только при
утечке, важно.

---

## 2. Устройства и push

### `PUT /devices/{device_id}/push-token`

```json
{ "fcm_token": "…" }
```

Токен FCM меняется сам по себе; клиент обновляет его при каждом изменении, а не
только при входе.

### `POST /devices/{device_id}/sync-token`

Добавлено ред. 10 по `05i`. Решение владельца 2026-08-15.

Ответ:

```json
{ "sync_token": "<jwt>", "expires_in": 7776000 }
```

**Зачем.** Фоновая половина приложения не может пользоваться refresh-токеном:
он ротируется при использовании, а экран и фоновый воркер предъявили бы его
одновременно — сервер увидел бы повтор, счёл кражей и погасил бы все сессии
устройства. Поэтому фону сеть запретили, и цена запрета оказалась смыслом
продукта: доза, подтверждённая из уведомления или пропущенная во сне, лежала на
телефоне, пока владелец не откроет приложение. Опекун не узнавал ничего.

Этот токен существует для фоновой половины. Два свойства делают его безопасным
для долгого хранения:

1. **Область — только `POST /sync/push` и `GET /sync/pull`.** На любом другом
   эндпоинте — `403 token_scope_insufficient`, включая выдачу нового такого же
   токена. Копия не может ни передать авторитет напоминаний, ни привязать
   опекуна, ни удалить аккаунт: то есть не может ни заставить звонить чужой
   телефон, ни заставить замолчать нужный.
2. **Умирает вместе с устройством.** Отвязка действует немедленно —
   `deps.current_caller` перечитывает строку устройства на каждом запросе, эта
   проверка существовала и раньше. Хранить список выданных токенов не нужно.

**Не ротируется** — в этом весь смысл, ротация и была причиной запрета.

**Живёт 90 дней**, выдаётся повторно сколько угодно раз, и выдача нового **не
гасит старый**. Иначе устройство, которое месяцами не открывали, — а это ровно
тот случай, ради которого механизм строится, — теряло бы доступ при попытке его
обновить.

Чем платим: ротации нет, значит украденную копию в работе не заметить. Принято
осознанно. На устройстве уже лежит refresh-токен, живущий столько же и умеющий
строго больше, — эта дверь не открывается заново, она уже открыта.

Клиенту: получать из foreground, где есть обычный access-токен, хранить в
Keystore рядом с refresh, обновлять при открытии приложения ближе к истечению.

### `POST /devices/{device_id}/heartbeat`

Отметка «устройство на связи». Именно она питает сигнал `profile_stale` (§5) —
без неё «нет данных» неотличимо от «нечего сообщать».

---

## 3. Pairing

Код генерирует **владелец профиля**: действие по расшариванию совершает тот, чьи
это медданные — самая чистая конструкция согласия под GDPR.

### `POST /pairing/codes`

```json
{ "profile_id": "<uuid>", "role": "with_alerts" }
```

Ответ:

```json
{ "code": "K7M-2QP", "expires_at": 1754800600000 }
```

- Роль (`viewer` | `with_alerts`) выбирается **при выдаче**, не при вводе: иначе
  принимающая сторона сама решала бы, сколько ей видно.
- Код одноразовый, TTL порядка 10 минут, в БД хранится хешем.
- Выдача — **запись о согласии**: кто, какой профиль, когда, с какой ролью.
  Хранится ради accountability, а не отладки.
- Только `owner` профиля. Иначе `403`.

### `POST /pairing/redeem`

```json
{ "code": "K7M-2QP" }
```

Создаёт членство и возвращает профиль в проекции наблюдателя (§4.3). Частота
попыток ограничена: короткий код без лимита перебирается.

### `POST /profiles/{id}/reminder-authority`

```json
{ "device_id": "<uuid>" }
```

**Ответ — `200` с телом** (ред. 14; до неё был `204` без тела):

```json
{ "owner_device_id": "<uuid>", "revision": 214 }
```

`revision` — `server_seq` профиля после операции: тот же номер, что уедет в push
`reminder_authority_lost` предыдущему устройству, и тот же, что придёт по `pull`.

**Почему тело появилось.** Номер сервер считал и раньше
(`RETURNING server_seq`), но отдавал только в nudge проигравшему — не тому, кто
выиграл. Заявитель поэтому не запоминал ревизию, и его забор от опоздавших push
оставался открытым на числе, которое он видел последним. Достижимо: устройство A
стоит на 5, сервер уже провёл A → B (6) → A (7), nudge про 6 задержан у FCM. A
заявляет авторитет, получает `204`, остаётся на 5. Приходит nudge 6, проходит
проверку «строго новее 5», и A снимает свои будильники и записывает владельцем B
— тогда как сервер числит владельцем A и больше ничего не пришлёт. Если B и есть
заменённый телефон, не звонит никто. Это тишина, а не терпимый дубль.

**Ветка «уже владелец» отвечает тем же телом.** Устройство, заявляющее авторитет,
который сервер за ним уже числит, — это ровно то, как выглядит устройство,
пропустившее передачу; молчание оставляло без номера того, кому он нужнее всех.
Возвращается **текущий** `server_seq` профиля, а не номер прошлой передачи: с тех
пор его могла сдвинуть обычная правка, и ответ старым числом снова открыл бы
забор.

⚠️ **`revision` здесь — JSON-число, а по `pull` — строка. Приводить их к одному
типу нельзя.** Опубликованная сборка читает это поле как `as num?` — на строке
бросает, причём внутри входа через Google; поле из `pull` она разбирает приёмом,
понимающим оба типа. А в `pull` оно обязано остаться строкой, потому что то же
значение едет вторым каналом через FCM `map<string,string>`, где чисел нет.
Прибито тестами `test_the_revision_in_the_receipt_is_a_number` и
`test_the_claim_answers_with_the_number_it_wrote`.

Передача авторитета напоминаний другому устройству. У профиля ровно одно
устройство материализует будильники (§1.4), поэтому это осознанная операция —
смена телефона или переезд профиля между устройствами одного аккаунта.

**Через sync push это делать нельзя, и поэтому там его нет.** Авторитет,
проходящий обычным потоком изменений, разрешался бы last-write-wins, а два
устройства, каждое из которых считает себя хозяином, — ровно то состояние,
против которого инвариант и существует. Один эндпоинт, один писатель, один
ответ.

Предыдущему устройству уходит push `reminder_authority_lost`, **но корректность
на нём не держится**. Истина — поле `owner_device_id` на профиле, которое каждое
устройство видит при ближайшем `pull`: увидев там не свой id, оно перестаёт
взводить будильники. Push лишь ускоряет это. Если бы инвариант зависел от
доставки push, потерянное уведомление означало бы два звонящих телефона по одной
дозе.

#### Тело nudge (ред. 11)

```json
{ "type": "reminder_authority_lost",
  "profile_id": "<uuid>",
  "owner_device_id": "<uuid нового владельца>",
  "revision": "214",
  "expires_at": "2026-08-18T13:00:00Z" }
```

**Все значения — строки.** Не стилистика: в FCM HTTP v1 `data` это
`map<string,string>`, и число, отправленное числом, всё равно приедет строкой.
`revision` — `"214"`, а не `214`.

| Поле | Что это | Зачем получателю |
|---|---|---|
| `owner_device_id` | новый владелец | отличить «ты потерял» от «потерял кто-то другой»: id, равный собственному, значит сообщение не про тебя |
| `revision` | `server_seq` профиля на момент передачи | сравнить с последним, что приехало по `pull`; строго не новее — отбросить |
| `expires_at` | ISO-8601 UTC, момент отправки + 1 час | вровень с TTL у FCM; сообщение, пережившее свой срок, описывает состояние, которое могло смениться |

**То же число обязано ехать по `pull`** — иначе забор мнимый: устройству не с
чем сравнивать, проверка «строго новее» пропускает любой push, и повторно
отправленный nudge способен отменить передачу, которая с тех пор была сделана
заново. См. `profiles.revision` в §4.3.

**Почему `server_seq`, а не отдельная колонка.** Он уже есть, уже монотонен и
уже сдвигается при передаче авторитета. То, что он растёт и в других случаях,
делает фильтр консервативным в безопасную сторону: передача на 205, push несёт
205; правка имени двигает до 260; устройство забирает 260 и запоминает вместе с
`owner_device_id`; опоздавший push на 205 отвергается — верно, потому что у
устройства уже более свежая правда.

**«В других случаях» — это не только правки.** `server_seq` — позиция в ленте, а
не счётчик изменений, и она сдвигается ещё и при повторной выдаче профиля: обмен
кода пары поднимает `server_seq` строкам профиля, лекарств и доз, не трогая
`updated_at` (это починка P0 от 15.08, `pairing._resend_profile`). Наблюдалось на
проде 18.08: `revision` профиля прошёл 214 → 414 при `updated_at` от 11.08.
Для правила «строго новее» это безвредно — ревизия только растёт, — но читать
`revision` как «сколько раз профиль меняли» нельзя.

#### Кто отправляет (ред. 12)

**Не API.** У процесса, отвечающего на запросы из интернета, нет учётных данных
FCM и не будет: `deploy/docker-compose.yml` отдаёт их только воркеру. До 18.08
API при этом *имел* отправителя, поэтому `build_push` возвращал ему логирующую
заглушку и каждый nudge уходил в файл. Теперь API кладёт строку в
`alert_deliveries` новым видом `reminder_authority_lost`, отправляет воркер.

Что это даёт помимо ключа в одном процессе: ретраи с backoff, `collapse_key`,
TTL и **запись о том, ушёл ли nudge** — вопрос, заданный на приёмке, на который
сервер не мог ответить. Цена — до 60 секунд на цикл воркера; против периода
фоновой синхронизации в 15–40 минут это ничто.

**Тело собирается в момент отправки, а не в момент постановки в очередь.** Это
не деталь реализации, а то, что делает ретраи безопасными. Со снимком повтор
через 20 минут говорит «ты не владелец» фактами двадцатиминутной давности: если
за это время владение вернулось, прежний владелец замолкает, будучи законным.
Собранное при отправке сообщение называет текущего держателя — и если это и есть
адресат, отправлять уже нечего.

Поэтому четыре случая, когда строка **снимается, а не повторяется**: профиль
удалён, авторитета нет ни у кого, адресат снова владелец, адресат вышел из
аккаунта. Все четыре — `expired`, и это отличимо от неудачной доставки: «nudge не
дошёл» и «nudge перестал быть правдой» — разные ответы, и виноват только первый.

`expires_at` в теле равен сроку самой строки, а не «час от каждой попытки»:
сервер не должен переставать следить за сообщением, которое FCM ещё держит.
`collapse_key` для nudge — `reminder_authority_lost:<profile_id>`, **без**
subject: два nudge по одному профилю не два разных сообщения, поздний заменяет
ранний, пока телефон офлайн.

#### Когда он уходит (ред. 13)

**Не сразу, а когда новый владелец подтвердил, что узнал о передаче.**

Замер 18.08 показал, почему. Nudge доходил до проигравшего за 1.4 с, тот гасил
цепочки за ~6 с — а победивший всё так же ждал свой фоновый прогон. Получалось
**2 мин 36 с, когда по дозе не звонил никто**. На сборке без клиентской починки
выходило 28 с, когда звонили оба. Оба состояния нарушают §1.4, но выбор между
ними не наш: инвариант №1 — надёжность напоминаний, **тишина хуже дубля**.

Поэтому сервер держит nudge, пока `devices.cursor_seq` нового владельца не
достигнет `server_seq` профиля. Пока не достиг, прежний телефон продолжает
звонить, и деградация — ограниченный дубль, а не провал.

`cursor_seq` — **копия последнего курсора, выданного устройству**, не источник
истины: курсор был и остаётся у клиента. Пишется в `pull`, только когда растёт;
повтор старого курсора его не двигает, иначе повторный запрос закрывал бы шлюз
перед устройством, которое уже догнало. `NULL` значит «ни одного pull не
известно» и держит nudge — осторожная сторона: подержать стоит дубля, отпустить
рано стоит тишины.

Сверка идёт с **текущим** `server_seq` профиля, а не с ревизией на момент
передачи: профиль, записанный ещё раз с тех пор, стоит выше, и pull,
остановившийся на старом числе, строки с новым владельцем не привозил.

Если победивший не выйдет на связь в течение часа, строка истекает,
nudge не уходит вовсе, и оба телефона расходятся по своим `pull` — то есть
поведение до всей этой машинерии. Тишины при этом не возникает.

**Чего это не чинит.** Nudge остаётся best-effort. Он доходит только до
устройства с живым токеном, которое сейчас забирает сообщения; для выключенного,
в глубоком Doze или force-stop оба устройства узнают о передаче только `pull`'ом,
в произвольном порядке. Замер 15.08: 49 секунд тишины либо 4 мин 36 с двойного
звона, направление решают два независимых расписания фоновых задач. Nudge делает
быстрым обычный случай, а не делает нарушение §1.4 недостижимым.

### `GET /profiles/{id}/members` · `DELETE /profiles/{id}/members/{account_id}`

Кто видит профиль и с какой ролью; отзыв — только владельцем. Отзыв немедленно
аннулирует refresh-токены устройств этого аккаунта **в части данного профиля** и
прекращает выдачу ему изменений.

---

## 4. Синхронизация

### 4.1 Курсор

Курсор — **непрозрачная строка**, выданная сервером. Внутри — монотонный
`server_seq`, присваиваемый сервером при записи.

Курсор **не строится на `updatedAt` устройства.** Часы на телефонах врут, а
совпадающие миллисекунды на границе выборки теряют записи навсегда и незаметно.
`updatedAt` используется, но для другого — для разрешения конфликтов (§4.4).

### 4.2 `POST /sync/push`

```json
{ "changes": {
    "profiles":    [ { "id": "…", "updated_at": 1754800000000, "deleted_at": null,
                       "name": "…", "color": 4283215696, "sort_order": 0 } ],
    "medications": [ … ], "schedules": [ … ],
    "dose_events": [ … ], "stock_events": [ … ] } }
```

#### Чего на проводе нет

Два поля исключены из синхронизации по разбору app-трека, и по разным причинам.

**`profiles.is_elder_mode`** — по собственному комментарию в коде это свойство
**устройства**, а не профиля: «this device renders the elder-mode UI for this
profile». На строке профиля оно живёт исторически, потому что в v1.0 устройство
было одно. Синхронизация разнесла бы настройку отображения одного телефона на
все устройства аккаунта: включил крупный шрифт на телефоне того, за кем следишь,
— получил его у себя.

**`medications.current_stock`** — кэш суммы журнала `stock_events`, о чём прямо
сказано в коде. App-трек предложил синхронизировать поле, но пересчитывать его на
клиенте после `pull`. Идём дальше: **не отправлять вовсе.** Производное значение
на проводе — это два источника истины, между которыми возможно расхождение;
единственный способ гарантировать, что они не разойдутся, — не иметь второго.
Клиент вычисляет остаток из журнала, сервер при необходимости сделает то же самое
из своего.

> **Следствие для применения изменений.** Поля, которых нет на проводе,
> **сохраняют локальное значение**. Применение пришедшей записи — это обновление
> известных полей, а не замена строки целиком: наивная замена обнулила бы
> `is_elder_mode` при первом же `pull`.

#### Никогда не синхронизируется

Локальное операционное состояние, к пользовательским данным не относящееся:
`scheduled_alarms`, `alarm_outbox`, `app_settings`, `materialization_revision`.

Запрет записан явно по просьбе app-трека — чтобы никто не добавил их «для
полноты». Синхронизация `alarm_outbox` между устройствами напрямую нарушила бы
§1.4: намерение поставить будильник уехало бы на устройство, которое хозяином
напоминаний не является.

Ответ:

Ответ различает **три исхода на запись**, но двумя списками: то, чего нет ни в
одном, применено — а это обычный случай, не заслуживающий строки на каждую из
тысячи.

```json
{ "cursor": "c2VxOjkxNDgy",
  "rejected": [ { "entity": "medications", "id": "…", "code": "forbidden_role" } ],
  "retry":    [ { "entity": "schedules",   "id": "…", "code": "missing_parent" } ] }
```

`rejected` — окончательно: повтор ничего не изменит, клиенту следует
остановиться и показать это. `retry` — не вина клиента (родитель ещё не доехал,
конфликт), и та же запись позже пройдёт. Свести их в один список значило бы
заставить клиента либо сдаться на поправимом, либо вечно повторять безнадёжное.

- Отправлять можно только изменения **своих** профилей. Наблюдаемый профиль
  доступен на чтение: `rejected` с `forbidden_role`, не `403` на весь запрос —
  одна лишняя запись не должна ронять весь батч.
- Батчи ограничены по размеру: **1000 записей суммарно по всем сущностям** и 2 МБ
  тела (потолок nginx). Превышение — **`413` `batch_too_large`**, и батч
  отвергается целиком, а не подрезается: частично применённый батч оставляет
  клиента в уверенности, что он отправил всё, и недостающие строки замечаются
  потом как данные, которые просто не доехали.

  ⚠️ **До ред. 14 здесь было написано, что размерный лимит возвращается `429` с
  `Retry-After`. Оба утверждения были ложью, и обошлись они дорого.** Размер
  всегда отвечал `413`; `429` приходил от nginx и означал частоту, а не размер;
  `Retry-After` не отправлялся вовсе. Клиент реализовал написанное честно — на
  `429` делил размер батча пополам, — то есть в ответ на «слишком часто» начинал
  ходить вдвое чаще. Дефект был в этом документе, а не в клиенте.

  Правило теперь однозначно: **`413` — про размер** (уменьшать батч), **`429` —
  про частоту** (ждать, размер не трогать).

#### Родитель записи неизменяем

Пять связей задаются при создании и после этого не меняются:

| сущность | поле | код отказа |
|---|---|---|
| `profiles` | `owner_account_id` | `forbidden_role` |
| `medications` | `profile_id` | `immutable_parent` |
| `dose_events` | `profile_id` | `immutable_parent` |
| `dose_events` | `medication_id` | `immutable_parent` |
| `schedules` | `medication_id` | `immutable_parent` |
| `stock_events` | `medication_id` | `immutable_parent` |

У дозы родителей **два**, и это единственная строка схемы, где их два. Первая
редакция правила закрыла только `profile_id`, то есть ту половину, которую никто
не атаковал (см. «почему» ниже).

Вставка с любым своим родителем разрешена как раньше; отвергается только смена
родителя у уже существующей строки. Форма отказа — та же, что у
`forbidden_role`:

```json
{ "cursor": "c2VxOjkxNDgy",
  "rejected": [ { "entity": "medications", "id": "…", "code": "immutable_parent" } ],
  "retry":    [] }
```

У профиля код другой не по прихоти: строка на диске принадлежит другому
аккаунту, и это факт о праве, а не об инварианте связи между нашими строками.

**Отвергается вся строка, атомарно.** Ни одно другое поле не применяется, даже
если приехало в той же записи; `server_seq` не двигается, и события в `pull` не
возникает. Иначе клиент получил бы половину версии: имя, приехавшее вместе с
родителем, который не приехал. Одна такая запись не роняет страницу — остальные
применяются как обычно.

**Отказ не зависит от того, выигрывает ли запись по LWW.** Устаревшая запись,
меняющая родителя, тоже возвращается в `rejected`, а не отбрасывается молча:
молчание клиент читает как «применено» и снимает строку с отправки.

**Порядок проверок:** если родитель, которого запись называет, вообще не
принадлежит вызывающему — `forbidden_role`, а не `immutable_parent`. Оба
окончательны, оба ведут в карантин. У дозы порядок такой: право на профиль,
право на лекарство, неизменяемость родителей — поэтому за одной клиентской
ошибкой могут прийти оба кода.

#### «Родителя ещё нет» и «родитель чужой» — разные ответы

| у записи не хватает родителя | ответ | что делать клиенту |
|---|---|---|
| родителя нет на сервере вовсе | `retry` · `missing_parent` | оставить в очереди, приедет с родителем |
| родитель есть, но чужой | `rejected` · `forbidden_role` | остановиться, карантин |

Правило одно для всех рёбер: профильного (`medications.profile_id`,
`dose_events.profile_id`) и лекарственного (`schedules.medication_id`,
`stock_events.medication_id`, `dose_events.medication_id`).

**До ред. 19 профильный край отвечал `forbidden_role` в обоих случаях**, и это
теряло данные. App-трек замерил как: правка профиля даёт ему свежий `seq` в их
исходящем журнале и уносит его **за** собственных детей, граница батча падает
между ними, и лекарство уезжает на сервер раньше своего профиля. Окончательный
отказ у них уходит в карантин; карантин не переигрывается, а отвергнутая строка
не получает нового `server_seq`, поэтому и `pull` её не вернёт. Лекарство
оставалось жить на одном телефоне, молча.

Отказ `retry` в худшем случае оставляет строку ждать; отказ `rejected` в худшем
случае теряет её насовсем. Поэтому на неизвестном id теперь `retry`.

**Правило выбора кода, согласовано обоими треками 2026-09-09.** Код выбирается по
худшему случаю ошибки, а не по тому, что сервер знает. Окончательный отказ
допустим только там, где повтор доказанно не поможет **и** принимающая сторона
умеет показать отказ человеку. Вторая половина — поправка app-трека, и она
несущая: «потеряно навсегда» сегодня не свойство кода `rejected`, а свойство
клиента, у которого исходящий карантин не переигрывается и не показывается. Когда
это починят, цена `rejected` упадёт до «человек увидел и решил» — но до тех пор
правило читается со второй половиной, иначе через год кто-нибудь применит его к
клиенту, который умеет показывать, и решит, что первая половина избыточна.

Возражение, что так сервер выдаёт наружу существование чужих id, не проходит
проверки фактом: `push` отвечает ровно это про **любой** id лекарства с самого
начала — иначе `schedules` и `stock_events` не могли бы быть retryable, — так что
ред. 19 не добавляет нового ответа, а делает профильный край таким же, как
остальные. Идентификаторы — v4 uuid, перебирать их нечем.

Клиентскую сторону это не отменяет: батч, отправляющий ребёнка раньше родителя,
всё ещё стоит закрыть по родителям (пункт 7 в §10), потому что порядок внутри
одного `push` сервер уже соблюдает, а между `push`'ами — нет.

**Карантин сам не рассасывается, и рассчитывать на `pull` тут нельзя.**
Отвергнутая строка не получает нового `server_seq` — в этом весь смысл
атомарного отказа, — значит `pull` с уже прошедшим курсором её не пришлёт.
Сервер не «починит» расхождение следующей страницей: восстановление, если оно
нужно, делает клиент, и это отдельная работа, а не автоматика. Ни этот документ,
ни ответ `push` ничего иного не обещают.

**Дополнительно: `dose_events.medication_id` теперь проверяется.** Доза называет
три вещи, а право на профиль, который она называет, ничего не говорило о
лекарстве, на которое она указывает: им могла быть любая строка таблицы, в том
числе чужая. Теперь лекарство обязано лежать в профиле вызывающего, иначе
`forbidden_role`; лекарство, которое ещё не доехало, как и раньше — `retry` с
`missing_parent`, причинный порядок не ломается. `schedule_id` не проверяется:
он nullable, обнуляется вместе с расписанием, и его идентификаторы не покидают
устройство владельца.

**Почему по каждой связи.**

`medications.profile_id` — на телефоне `dose_events.profile_id` пишется при
материализации дозы и потом не переписывается: у лекарства, сменившего родителя,
дозы продолжают называть прежний профиль. Реконсилятор судит об авторитете
напоминаний по этому полю, а не по текущему родителю лекарства, отложенная
постановка будильника отбрасывается как устаревшая — и телефон молчит. Ошибки
нет нигде: отказ виден только пропущенной дозой.

`dose_events.profile_id` — из пяти эта дыра была самой доступной. Идентификаторы
доз выдаются наблюдателю по построению (§4.3), поэтому опекун мог прислать чужую
дозу под своим профилем, и строка уходила из ленты владельца в его собственную.

`dose_events.medication_id` — тише и хуже, чем перенос профиля. Авторитет
напоминания читается из `profile_id`, который при этом не меняется, поэтому
телефон продолжает звонить и звонить вовремя. Но текст напоминания собирается
join'ом через `dose_events.medication_id`, а подтверждение приёма списывает запас
у того же лекарства: получается правильный будильник о неправильном лекарстве и
неправильная пачка, посчитанная вниз. Ошибки нет нигде, история сходится сама с
собой. Обе строки кода назвал ревьюер app-трека 09.09; воспроизведено на сервере
до того, как принято на веру: один аккаунт, без всякой гонки — доза лежит как
(p1, m1), push присылает её как (p1, m2), обе проверки права проходят, профиль не
менялся, триггер молчит.

`profiles.owner_account_id` — проверка спрашивала, **видит** ли вызывающий
профиль, а опекун с отозванным доступом его уже не видит, но идентификатор помнит.
Пуш строки профиля переписывал владельца на него: дальше он тянул проекцию
владельца — имена, заметки и расписания, которых наблюдателю не выдают вообще, —
а у настоящего владельца профиль пропадал из `roles`. То есть отзыв доступа не
просто не работал, а был ступенькой: пока опекун в членстве, старая проверка
отказывала; после отзыва — не отказывала ничего.

`schedules.medication_id` — перепривязка расписания оставляет уже
материализованные дозы называть то лекарство и тот профиль, для которых они были
построены.

`stock_events.medication_id` — остаток есть сумма журнала, поэтому перенос одного
события молча меняет два остатка.

Легального переноса нет ни у одной из пяти связей: передача reminder-authority
пишет `owner_device_id`, а не `owner_account_id`; пара даёт членство, а не
владение; переноса лекарства, расписания или события склада между родителями в
приложении нет.

**Запрет живёт в схеме** — по одному `BEFORE UPDATE` триггеру на таблицу,
миграции 0014 и 0015, — а не в обработчике `push`. Это то, что делает отказ
атомарным: оператор падает целиком, поэтому «применить остальные поля»
физически невозможно. И то же правило действует для любого другого писателя, а
не только для этого эндпоинта.

Правило принято ревьюером кода app-трека 2026-09-09 для лекарств (владелец
подтвердил объём работ, не саму формулировку) и в тот же день распространено
backend-треком на остальные четыре связи по итогам проверки — она показала, что
это была одна ошибка, написанная пять раз. Порядок выкладки — сначала сервер,
затем клиентский guard и карантин.

#### Правило удалений

**Ничто не исчезает с провода молча. Любое удаление — `deleted_at`.**

Первая редакция утверждала, что модель это уже умеет. App-трек показал, что для
`dose_events` это неверно: `DoseMaterializer._purgePending` физически удаляет
будущие pending-дозы при правке расписания, и без tombstone ближайший `pull`
воскресил бы то, что устройство убрало сознательно — пользователь поменял время
приёма, а старые дозы всплыли обратно.

Принят **вариант A**: пурж расписания переводится на `deleted_at`. Правка на
стороне приложения, протокол остаётся чистым.

**Retention — и здесь правило шире, чем предложил app-трек.**

Для `dose_events` независимая чистка по возрасту на обеих сторонах безопасна: от
их полноты ничего не вычисляется.

Для `stock_events` — **нет**, и это разобрано по коду. `_retentionCleanup`
сворачивает вычищаемые события в **новое** `stock_event` с суммой (baseline) и
удаляет старые физически. Если такой baseline уедет на сервер, где исходные
события ещё лежат, сумма журнала удвоится, и любое свежее устройство посчитает
неверный остаток. Отказ тихий: ни исключения, ни расхождения статусов — просто
неправильное число.

Поэтому сворачивание обязано **пересекать провод целиком**: вставка baseline
плюс `deleted_at` на свёрнутых событиях. Тогда обе стороны приходят к одному
журналу, а сервер не выполняет собственной арифметики свёртки — по той же
причине, по которой не разворачивает расписания (§0): вторая реализация той же
логики разойдётся с первой, и разойдётся тихо.

Сервер вправе физически удалять записи, у которых `deleted_at` старше 18
месяцев. Для клиентов это невидимо: свежее устройство получает уже свёрнутое
состояние.

**Свёртку выполняет только устройство с reminder-authority.**

Спека §1.1 разрешает аккаунту иметь несколько устройств, а `_retentionCleanup` —
локальный свип, который запускает то устройство, которое проснулось. Значит при
двух своих телефонах на одном профиле оба свернут журнал независимо и отправят
по своему baseline. Сумма удвоится снова — на этот раз без всякого участия
сервера.

Правило поэтому такое: **порождать** свёртку вправе только хозяин напоминаний
(§1.4); **применять** пришедшую — любое устройство. Остальные получают baseline и
надгробия по sync и вправе физически убрать у себя пришедшее удалённым, но
собственный baseline не создают никогда.

Опекунов это не касается: при статус-only `stock_events` до них не доходят
(§4.3), так что порождать свёртку им попросту не из чего.

### 4.3 `GET /sync/pull?cursor=…`

```json
{ "cursor": "c2VxOjkxNTAx", "has_more": false,
  "changes": { "profiles": [ … ], "medications": [ … ],
               "schedules": [ … ], "dose_events": [ … ], "stock_events": [ … ] } }
```

#### `account_id` в каждом push (ред. 21)

Полезная нагрузка и caregiver-алерта, и nudge о потере авторитета несёт
`account_id` **получателя**, строкой. Доставка at-least-once с TTL в часы, значит
push может прийти после того, как человек вышел из этого аккаунта и вошёл в
другой; до ред. 21 отличить такое сообщение было нечем. Клиент сверяет поле с
текущим аккаунтом и молча выбрасывает чужое. Старые клиенты лишнее поле
игнорируют — поэтому серверная половина выкладывается первой, и это не
осторожность, а единственный возможный порядок: guard не с чем сверять, пока
поля нет.

У nudge это важнее, чем у алерта: он говорит телефону **перестать звонить**, и
переживший выход из аккаунта должен отвергаться на входе, а не разбираться по
содержимому.

#### `GET /sync/preview?cursor=…` — та же лента, без признания в том, что её прочли

Та же авторизация, та же видимость, та же проекция по роли, та же пагинация, то
же тело. Единственное отличие: **`Device.cursor_seq` не двигается** — и это
отличие и есть весь endpoint.

Клиенту нужно показать человеку, что лежит на сервере, **до** решения принимать
это или нет: какой профиль его, сколько в нём лекарств. Сделанное через
`/sync/pull`, это заставляло сервер считать, что устройству выдали строки,
которых оно не применяло и может не применить никогда. А `cursor_seq` — не
бухгалтерия: это то доказательство, которого ждёт шлюз передачи авторитета,
прежде чем сказать прежнему телефону замолчать (§3). То есть чтение вперёд через
`pull` могло **погасить телефон на основании страницы, которую только посмотрели**.

Отдельным маршрутом, а не флагом на старом: клиент, который про него не знает,
не может случайно получить его семантику, а старый клиент сохраняет поведение,
под которое написан. `/sync/pull` со старыми клиентами совместим без изменений.

Проверено тестами в обе стороны: несколько страниц preview не двигают
`cursor_seq` и **не выпускают revocation nudge** (шлюз остаётся закрытым, прежний
телефон продолжает звонить), а обычный pull после этого работает как прежде.

#### Что `cursor_seq` доказывает на самом деле — **открытый вопрос**

Обычный `pull` пишет `cursor_seq` **в момент выдачи ответа**: не когда ответ
дошёл, не когда транзакция применения закоммитилась, не когда поставлен хоть
один будильник. Байты ещё в пути, а сервер уже считает устройство осведомлённым.

Для всего остального это неважно, а для шлюза §3 — важно, потому что шлюз читает
эту колонку как «победитель готов». Потерянный по дороге ответ означает: прежний
телефон погашен, новый ничего не применил, не звонит никто. Это тишина, а
инвариант №1 ставит её ниже дубля — то есть шлюз в этом случае даёт ровно то, от
чего заведён. Достижимость записана тестом
`test_the_gate_opens_on_a_page_that_was_only_handed_out`.

**Одно уточнение, которое сужает задачу.** Передача авторитета пишет профилю
`server_seq = nextval(...)`, то есть **максимальный** на тот момент номер, а лента
`pull` упорядочена по `server_seq` глобально. Значит условие шлюза
`cursor_seq >= profile.server_seq` уже означает «устройству выдано всё, что
старше передачи» — включая лекарства, расписания и дозы, из которых звонить. На
оси «какие строки» многостраничность дыры не создаёт: строка профиля приезжает
не первой, а последней.

Остаётся ровно два разрыва, и они разной природы:

1. **«выдано» ≠ «получено».** Ответ мог не дойти. Лечится тем, что курсор
   двигают не по выданной странице, а по тому курсору, который клиент
   **присылает** следующим запросом: это доказательство получения, а не отправки.
2. **«получено и применено в БД» ≠ «будильники стоят».** Применение строк и
   материализация с постановкой будильников — разные шаги на клиенте, и второго
   сервер не видит ни в каком виде. Никакой курсор этого не докажет, потому что
   доказывать нечем: факт живёт на телефоне.

**Поэтому строгая форма — не «сдвинуть курсор позже», а отдельный сигнал
готовности.** Клиент сообщает: «применил до курсора N, материализовал, будильники
по профилю сверены». Сервер хранит это отдельно от `cursor_seq` (например
`ready_seq`), а шлюз читает готовность, а не выдачу.

**Что ответил клиентский трек 17.09** (через владельца; прямого канала между
треками больше нет). Курсор у них сохраняется в той же SQLite-транзакции, что и
обработка страницы, — но это не «всё применено»: отдельные строки могут остаться
в карантине или ждать родителя, а следующая страница при пагинации запрашивается
**до** пересборки будильников. То есть подтверждённый курсор не годится даже как
доказательство применения, не говоря о готовности.

**Готовность подтверждается по конкретному профилю**, и вот её определение, их
формулировка: нужные строки применены, материализация и сверка будильников
прошли, для профиля нет нерешённых входящих строк и нет незавершённых постановок
в alarm outbox. Единого такого сигнала в клиенте сейчас нет — его предстоит
сделать обеим сторонам.

**Ловушка перехода — и моё первое правило было неверным.** Я предлагал: нет
`ready_seq` — читать `cursor_seq`. Клиентский трек показал, что это открывает
преждевременно **именно первую передачу**: у нового клиента до первого сигнала
`ready_seq` пуст, и шлюз откроется по курсору ровно там, где строгость нужнее
всего. Поэтому клиент включает протокол **до первого обычного `pull`**, а сервер
обязан знать о поддержке отдельно от факта готовности. Обратная ловушка остаётся
в силе: у клиента, который сигнала не шлёт вообще, шлюз читает `cursor_seq`, иначе
прежний телефон звонил бы вечно.

**Форма — отдельный идемпотентный endpoint, не heartbeat**, и при приёме сервер
сверяет текущего владельца и ревизию: готовность по профилю имеет смысл только от
того устройства, которое им владеет, и только про ту ревизию, о которой речь.

**Статус: спроектировано, не реализовано.** `/sync/preview` из ред. 20 убирает
только один случай — чтение вперёд ради выбора профиля; обычный `pull`
по-прежнему двигает `cursor_seq` до постановки будильников. Ни один из двух
endpoint'ов ред. 20 этот P0 не закрывает, и считать иначе нельзя.

**Лимиты — в §4.2**, одним местом. Раньше они были описаны и здесь, и там,
причём по-разному: тут `413`, там `429`. Клиент реализовал ту половину, которая
была неверной, и это дало P0. Дублировать факт о проводе значит однажды
разойтись в нём.

**Каждая запись несёт `op_seq`** — монотонный счётчик устройства, не зависящий
от часов. Вместе с `origin_device_id`, который сервер берёт **из токена, а не из
тела**, он образует личность операции.

Сравнение идёт по тройке `(updated_at, origin_device_id, op_seq)`, и это делает
сразу две вещи. Повтор той же операции — точная ничья: ничего не переписывается,
`server_seq` не меняется, строка не рассылается остальным устройствам. А две
правки одной строки в одну миллисекунду различимы, потому что `op_seq` у них
разный.

> **Отмена прежней формулировки.** Ред. 4 обещала идемпотентность по строгому
> «`updated_at` больше текущего». Это было неверно и теряло данные: вторая
> правка в ту же миллисекунду принималась за повтор первой и молча исчезала.
> Ошибку нашёл app-трек. Сравнение по одному лишь времени не может одновременно
> распознать повтор и различить две правки — для этого нужна личность операции.

**Причинный порядок НЕ гарантируется.** Родитель может приехать позже ребёнка,
и клиенту нужна очередь ожидающих родителей.

Причина не в лени. `pull` отдаёт текущую версию строки под её последним
`server_seq`, а не историю. Профиль, созданный первым и переименованный вчера,
имеет `server_seq` выше, чем лекарство внутри него, — и при выборке с нуля
лекарство придёт раньше профиля. Гарантировать иное можно было бы, лишь рассылая
родителей повторно, то есть сломав саму идею дельты.

Сортировать страницу по типам сущностей, чтобы «обычно» родитель шёл первым, я
сознательно не стал. Тогда очередь ожидающих срабатывала бы редко — то есть
почти не проверялась бы, а сработала в редком случае и с ошибкой. Пусть лучше
она работает всегда и потому остаётся исправной.

Записи, чей родитель ещё не приехал, `push` возвращает в `retry`, а не в
`rejected` (§4.2).

**Постраничность обязательна.** Клиент обязан крутить `has_more` до конца; сервер
режет страницу на своё усмотрение и вправе изменить её размер без bump'а версии.
Отдельного «первичного sync» нет: первый `pull` с пустым курсором отдаёт всё,
постранично. Объём конечен — retention держит историю в 18 месяцах, верхняя
оценка для тяжёлого пользователя порядка 13 тысяч `dose_events` (расчёт
app-трека).

На проводе всегда присутствуют до **трёх суток будущих `pending`-доз**:
материализатор работает скользящим окном 72 часа. Это те самые записи, которые
удаляются при правке расписания, — поэтому окно и правило удалений связаны.

**Выдача зависит от роли** — это прямое следствие решения «только статус» (§1.6):

| Сущность | Свой профиль (`owner`) | Наблюдаемый (`viewer`, `with_alerts`) |
|---|---|---|
| `profiles` | целиком, включая `owner_device_id`, `revision` и `role` | `id`, `name`, **`role`** |
| `medications` | целиком | `id`, `profile_id`, `name`, `form` |
| `schedules` | целиком | **не выдаётся** |
| `dose_events` | целиком | `id`, `medication_id`, `profile_id`, `planned_at`, `status`, `action_at` |
| `stock_events` | целиком | **не выдаётся** |

Не выдаются наблюдателю: расписания, остатки (`current_stock`, `pack_size`),
заметки, фото, пороги пополнения, а также `owner_device_id` и `revision` — какое
устройство звонит по чужому профилю его не касается, а ревизия существует ровно
затем, чтобы решать, продолжать ли взводить будильники. Наблюдатель не взводит
ни одного.

**`profiles.revision`** (ред. 11) — строка с десятичным числом, `"214"`.
Монотонна в пределах профиля, растёт при любой записи в него. Единственное её
назначение: сравнение с `revision` из push `reminder_authority_lost` (§3).
Устройство хранит последнюю приехавшую и отбрасывает push, чей `revision` не
строго больше. Строка, а не число, потому что во втором канале — FCM — тип не
наш, а сравнивать надо одно и то же значение из обоих.

**Передача авторитета не трогает `updated_at`** — гарантия контракта, а не деталь
реализации. `updated_at` — то, что сравнивает last-write-wins. Двигай его
передача, сервер сочинял бы отметку времени содержимого, и собственное
неотправленное переименование профиля на телефоне проиграло бы правке, которой
никто не делал.

У этого есть следствие на другой стороне провода, и оно стоило app-треку P0:
после передачи входящий `updated_at` **равен** локальному по построению. Клиент,
применяющий `owner_device_id` только когда содержимое разрешилось как «новее»,
отбросит вообще все передачи, которые ему когда-либо пришлют. Владение — не
правка профиля; применять его следует независимо от исхода LWW по содержимому.

**Строка профиля возвращается в ленту при каждой передаче.** `server_seq`
сдвигается, значит строка снова попадает в выборку `> cursor` — даже когда
содержимое не менялось. Это и есть механизм доставки, push лишь ускоряет его.
Прибито тестами `test_a_handover_does_not_touch_updated_at` и
`test_a_handover_alone_puts_the_profile_back_in_the_feed`; второй сначала
вычерпывает курсор, иначе он доказывал бы только то, что `pull` с нуля отдаёт всё.

**Но `role` выдаётся всем, и это вопрос P0, а не удобства.** Без неё наблюдатель
видит профиль без `owner_device_id` и не может отличить его от собственного
профиля, который ещё никто не заявил, — то есть начал бы взводить будильники по
чужим дозам. С ролью правило не имеет неоднозначной ветки:

```
role != owner                        никогда не взводить
role == owner, owner_device_id пуст  заявить авторитет, затем взводить
role == owner, id == это устройство  взводить
role == owner, id == другое          не взводить
```

Членства (`profile_memberships`) на проводе нет и не будет: наблюдателю оно
раскрыло бы, кто ещё видит профиль, а роль отвечает на единственный вопрос,
который устройству нужно задать.

**Почему расписаний нет.** Это не про экономию трафика. Без расписаний
устройству наблюдателя нечего материализовать в будильник, и инвариант §1.4
(ровно один хозяин напоминаний) держится **структурно**, а не корректностью кода
подавления. При полной репликации одна ошибка в подавлении означала бы звонок не
на том телефоне — прямой удар по P0.

Названия лекарств наблюдателю **уходят**: без них алерт бесполезен. Это
осознанный минимум, а не недосмотр (Часть 2.4).

#### Поле `roles` — куда класть строку, без догадок

Добавлено ред. 6 по запросу app-трека. Каждый ответ `pull` несёт карту всех
видимых вызывающему профилей:

```json
"roles": { "<profile_id>": "owner" | "viewer" | "with_alerts" }
```

Присутствует **всегда**, в том числе когда `changes` пуст, и не зависит от
курсора.

Зачем: строка называет свой `profile_id`, но строка самого профиля может лежать
на страницы позади — порядок по `server_seq`, и профиль, правленный полгода
назад, отсортируется далеко после сегодняшних доз. Ждать её означало бы держать
дозы в ожидании неопределённое время.

**Определять принадлежность по форме записи нельзя.** «Нет `schedule_id` —
значит наблюдаемая» неверно: у собственной дозы это поле тоже бывает пустым.
Доза, принятая вне расписания, не имеет его вовсе, и на проводе оно объявлено
`schedule_id: uuid | null`. Такой маршрут отправил бы собственные внеплановые
дозы владельца в чужую ветку и погасил бы по ним напоминания. Закреплено тестом
`test_an_owned_dose_may_have_no_schedule`.

Роль на связке — единственный авторитетный ответ, и `roles` даёт его сразу для
всех строк страницы. Тест `test_every_pull_says_which_profiles_are_whose`.

**Исчезновение профиля из `roles` — сигнал отзыва доступа, и клиент обязан по
нему удалить локальные данные этого профиля.**

Это вторая работа, которую делает карта, и она не про удобство. Отзыв членства
до сих пор выражался только тем, что строки переставали приходить, — а «перестали
приходить» неотличимо от «на той стороне тихая неделя». Данные о здоровье
оставались на устройстве опекуна после того, как согласие, под которым они были
переданы, отозвано. Молчание не может быть формой уведомления об отзыве.

Теперь профиль есть в `roles` на одном `pull` и отсутствует на следующем.
Отсутствие — инструкция удалить: наблюдаемую историю, названия лекарств, имя
профиля. Сигнал одинаково покрывает отзыв членства владельцем, удаление профиля
и удаление аккаунта владельца — во всех трёх случаях профиль просто перестаёт
быть видимым.

Тесты `test_a_revoked_profile_disappears_from_roles` и
`test_revoking_a_membership_stops_the_data`.

#### Что из этого следует для схемы наблюдателя

Таблица выше — не оптимизация, а форма данных, в которой наблюдатель их только и
получит. Локальная схема на его стороне обязана эту форму принимать, иначе
история доз у опекуна не сохранится вовсе.

Куда именно класть наблюдаемую историю — отдельная read-only таблица или та же
`dose_events` — решает app-трек: это их сторона шва. Отдельная таблица
предпочтительнее, и по их же доводу: тогда §1.4 держится структурно с обеих
сторон, материализатор сканирует `dose_events`, и чужих доз там не существует.
**Schedule-заглушки не будет** — строка, которой не соответствует ничего на
сервере, рано или поздно уедет в push и станет чужой записью в чужом профиле.

Конкретно, и это **блокирующее** требование:

1. **`dose_events.schedule_id` у наблюдателя отсутствует всегда.** Он не «иногда
   пустой» — его нет в проекции. Колонка на устройстве должна быть nullable и
   **без внешнего ключа на `schedules`**: расписаний наблюдатель не получает
   никогда, поэтому ссылаться этому ключу не на что по построению, а не по
   стечению обстоятельств.
2. **`medications` приходят в четырёх полях.** `dose_amount`, `pack_size`,
   `notes`, `photo_key`, `refill_threshold_days`, `is_active` не придут —
   значит, и они nullable, и ни одно вычисление на устройстве не вправе их
   потребовать.
3. **`profiles` приходят без `owner_device_id` и без `revision`.** Роль вместо
   первого, см. выше; второе наблюдателю не нужно ни для чего.
4. **`updated_at` и `deleted_at` наблюдателю приходят.** Проекция режет поля
   сущности, но эти два добавляются обратно всегда — без первого нет LWW, без
   второго не доедут удаления. Видно в теле §4.6 и в `_project`.
   `created_at` не приходит: он ни в чём не участвует. Нужен — скажите, поле не
   чувствительное.
5. **Наблюдаемая доза никогда не станет полной.** Владение профилем не
   передаётся между аккаунтами: `owner_account_id` проставляется по вызывающему
   и push от невладельца отклоняется `forbidden_role`. Наблюдатель владельцем не
   становится, и одна и та же доза не приедет сначала усечённой, а потом полной.
   (Роль может меняться `viewer` ↔ `with_alerts` — проекция у них одна и та же.)

Проверено на реализации app-трека 2026-08-13: `lib/core/db/tables.dart:137`
объявляет `scheduleId` как `text().references(Schedules, #id)()` — NOT NULL и
внешний ключ. При такой схеме вставка первой же строки `dose_events`
наблюдателя падает: сначала на NOT NULL, а если снять — на FK. Требование
внесено сюда именно потому, что таблица проекции сама по себе этого не говорила.

Обратное требование к серверу: набор полей проекции — часть контракта, а не
деталь реализации. Поле, добавленное в owner-строку, **не** попадает
наблюдателю автоматически; расширение проекции — изменение контракта.
Закреплено `WATCHER_FIELDS` и тестами `test_a_watcher_never_receives_schedules`,
`test_a_watcher_gets_no_stock_or_notes`.

### 4.4 Конфликты

Сервер — авторитет (§4.3 спеки). Правило: **last-write-wins по записи целиком**,
по `updated_at` устройства; при равенстве выигрывает пришедшее позже по
`server_seq`.

Не по полям: слияние по полям создаёт состояния, которых не было ни на одном
устройстве — лекарство с новой дозировкой и старой формой. Для двух телефонов в
одной семье, правящих одно лекарство одновременно, выигрыш от слияния
воображаемый, а вред реальный.

**Исключение: связь с родителем.** `profiles.owner_account_id`,
`medications.profile_id`, `dose_events.profile_id`, `dose_events.medication_id`,
`schedules.medication_id` и `stock_events.medication_id` неизменяемы после
создания (§4.2), поэтому запись, меняющая любое из них, в last-write-wins не участвует вовсе — она отвергается и
более новой, и более старой. LWW выбирает между версиями строки; смена родителя
— не версия той же строки, а перенос её в другое место.

`dose_events` — не чисто append: запись создаётся как `pending` и переходит в
`taken`/`skipped`/`missed`. То же правило LWW. App-трек подтвердил: код прав,
неточен был комментарий в `tables.dart`, потерявший оговорку ТЗ §4.4.3.
Действительно append-only — `stock_events`, правки там компенсирующими событиями.

**Статус не монотонен, и сервер не вправе это предполагать.** Разрешены и
обратные переходы: `missed → pending` автоматическим свипом
`reviveMissedWithinWindow`, когда пользователь расширил окно приёма, и
`taken → pending` отменой из календаря. Откат статуса — штатное поведение, а не
признак рассинхрона; любое правило вида «более поздний статус выигрывает»
сломается на нём. Выигрывает более поздний `updated_at`, а не «более
продвинутый» статус.

**Известное ограничение: LWW опирается на часы устройства.** Курсор на них не
строится (§4.1), а конфликт-резолюшен — да. Приложение этим часам внутри себя уже
не доверяет: счётчик `materialization_revision` существует именно потому, что
перевод часов назад — реальный случай. Практически риск мал, `dose_events` пишет
один владелец профиля (§1.4). Но при двух своих устройствах телефон с уехавшими
часами будет систематически выигрывать. Записано как ограничение осознанно:
векторные часы при неподтверждённом спросе (§0.3) — не тот размен.

### 4.6 Реальные тела — снятые с работающего сервера

Ниже не описания, а **вывод `api/tools/contract_examples.py`**, который поднимает
приложение, выполняет настоящие запросы и печатает то, что реально ушло и
пришло. Перегенерировать — та же команда; расхождение примера с кодом означает,
что один из них устарел, и это видно сразу.

Первый же прогон на настоящих значениях нашёл дефект: `profiles.color` был
`INTEGER`, а цвет Android — беззнаковый ARGB (`0xFF2A9D8F` = `4283215696`),
который в int32 не влезает. Синтетические нули в тестах это скрывали. Миграция
0005.

[2m2026-08-18T10:20:03.431369Z[0m [[32m[1minfo     [0m] [1mhttp                          [0m [[0m[1m[34mapi.request[0m][0m [36mmethod[0m=[35mPOST[0m [36mms[0m=[35m65[0m [36mpath[0m=[35m/v1/auth/google[0m [36mstatus[0m=[35m200[0m
HTTP Request: POST http://x/v1/auth/google "HTTP/1.1 200 OK"
[2m2026-08-18T10:20:03.442368Z[0m [[32m[1minfo     [0m] [1mhttp                          [0m [[0m[1m[34mapi.request[0m][0m [36mmethod[0m=[35mPOST[0m [36mms[0m=[35m9[0m [36mpath[0m=[35m/v1/auth/google[0m [36mstatus[0m=[35m200[0m
HTTP Request: POST http://x/v1/auth/google "HTTP/1.1 200 OK"

#### POST /v1/sync/push — тело

```json
{
  "changes": {
    "profiles": [
      {
        "id": "6a1af946-74bd-4b19-a210-fdf46e1cf525",
        "created_at": 1787048403443,
        "updated_at": 1787048403443,
        "deleted_at": null,
        "op_seq": 1,
        "name": "Mum",
        "color": 4283215696,
        "sort_order": 0
      }
    ],
    "medications": [
      {
        "id": "fcf52009-1ae9-46d0-bccf-25ef120d8cdd",
        "created_at": 1787048403443,
        "updated_at": 1787048403443,
        "deleted_at": null,
        "op_seq": 2,
        "profile_id": "6a1af946-74bd-4b19-a210-fdf46e1cf525",
        "name": "Enalapril",
        "notes": null,
        "dosage_text": "10 mg",
        "dose_amount": 1.0,
        "form": "tablet",
        "pack_size": 30.0,
        "refill_threshold_days": 3,
        "is_active": true,
        "photo_key": null
      }
    ],
    "schedules": [
      {
        "id": "0c90a1ea-a155-45da-9061-205fbdf54a8f",
        "created_at": 1787048403443,
        "updated_at": 1787048403443,
        "deleted_at": null,
        "op_seq": 3,
        "medication_id": "fcf52009-1ae9-46d0-bccf-25ef120d8cdd",
        "type": "fixed_times",
        "times": "[\"09:00\",\"21:00\"]",
        "days_of_week": null,
        "interval_days": null,
        "start_date": "2026-08-01",
        "end_date": null
      }
    ],
    "dose_events": [
      {
        "id": "cd033526-8a4f-49a8-8937-704d9741f392",
        "created_at": 1787048403443,
        "updated_at": 1787048403443,
        "deleted_at": null,
        "op_seq": 4,
        "schedule_id": "0c90a1ea-a155-45da-9061-205fbdf54a8f",
        "medication_id": "fcf52009-1ae9-46d0-bccf-25ef120d8cdd",
        "profile_id": "6a1af946-74bd-4b19-a210-fdf46e1cf525",
        "planned_at": 1787048403443,
        "status": "pending",
        "action_at": null,
        "snooze_count": 0,
        "snoozed_until": null,
        "dose_amount": 1.0
      }
    ],
    "stock_events": [
      {
        "id": "c3a62154-eb37-4366-87f7-f58344a8c3b6",
        "created_at": 1787048403443,
        "updated_at": 1787048403443,
        "deleted_at": null,
        "op_seq": 5,
        "medication_id": "fcf52009-1ae9-46d0-bccf-25ef120d8cdd",
        "delta": 30.0,
        "reason": "manual",
        "dose_event_id": null
      }
    ]
  }
}
```
[2m2026-08-18T10:20:03.526771Z[0m [[32m[1minfo     [0m] [1mhttp                          [0m [[0m[1m[34mapi.request[0m][0m [36mmethod[0m=[35mPOST[0m [36mms[0m=[35m82[0m [36mpath[0m=[35m/v1/sync/push[0m [36mstatus[0m=[35m200[0m
HTTP Request: POST http://x/v1/sync/push "HTTP/1.1 200 OK"

#### POST /v1/sync/push — ответ

```json
{
  "cursor": "c2VxOjU=",
  "rejected": [],
  "retry": []
}
```
[2m2026-08-18T10:20:03.537937Z[0m [[32m[1minfo     [0m] [1mhttp                          [0m [[0m[1m[34mapi.request[0m][0m [36mmethod[0m=[35mPOST[0m [36mms[0m=[35m9[0m [36mpath[0m=[35m/v1/auth/google[0m [36mstatus[0m=[35m200[0m
HTTP Request: POST http://x/v1/auth/google "HTTP/1.1 200 OK"
[2m2026-08-18T10:20:03.547052Z[0m [[32m[1minfo     [0m] [1mhttp                          [0m [[0m[1m[34mapi.request[0m][0m [36mmethod[0m=[35mPOST[0m [36mms[0m=[35m8[0m [36mpath[0m=[35m/v1/sync/push[0m [36mstatus[0m=[35m200[0m
HTTP Request: POST http://x/v1/sync/push "HTTP/1.1 200 OK"

#### POST /v1/sync/push — отказ по конкретной записи

```json
{
  "cursor": "c2VxOjU=",
  "rejected": [
    {
      "id": "8f75ea4f-82af-4dc8-bd29-43944ab80472",
      "entity": "medications",
      "code": "forbidden_role"
    }
  ],
  "retry": []
}
```
[2m2026-08-18T10:20:03.599120Z[0m [[32m[1minfo     [0m] [1mhttp                          [0m [[0m[1m[34mapi.request[0m][0m [36mmethod[0m=[35mGET[0m [36mms[0m=[35m50[0m [36mpath[0m=[35m/v1/sync/pull[0m [36mstatus[0m=[35m200[0m
HTTP Request: GET http://x/v1/sync/pull "HTTP/1.1 200 OK"

#### GET /v1/sync/pull — ответ владельцу (без курсора: всё)

```json
{
  "cursor": "c2VxOjU=",
  "has_more": false,
  "changes": {
    "profiles": [
      {
        "id": "6a1af946-74bd-4b19-a210-fdf46e1cf525",
        "updated_at": 1787048403443,
        "deleted_at": null,
        "created_at": 1787048403443,
        "name": "Mum",
        "color": 4283215696,
        "sort_order": 0,
        "owner_device_id": null,
        "revision": "1",
        "role": "owner"
      }
    ],
    "medications": [
      {
        "id": "fcf52009-1ae9-46d0-bccf-25ef120d8cdd",
        "updated_at": 1787048403443,
        "deleted_at": null,
        "created_at": 1787048403443,
        "profile_id": "6a1af946-74bd-4b19-a210-fdf46e1cf525",
        "name": "Enalapril",
        "notes": null,
        "dosage_text": "10 mg",
        "dose_amount": 1.0,
        "form": "tablet",
        "pack_size": 30.0,
        "refill_threshold_days": 3,
        "is_active": true,
        "photo_key": null
      }
    ],
    "schedules": [
      {
        "id": "0c90a1ea-a155-45da-9061-205fbdf54a8f",
        "updated_at": 1787048403443,
        "deleted_at": null,
        "created_at": 1787048403443,
        "medication_id": "fcf52009-1ae9-46d0-bccf-25ef120d8cdd",
        "type": "fixed_times",
        "times": "[\"09:00\",\"21:00\"]",
        "days_of_week": null,
        "interval_days": null,
        "start_date": "2026-08-01",
        "end_date": null
      }
    ],
    "dose_events": [
      {
        "id": "cd033526-8a4f-49a8-8937-704d9741f392",
        "updated_at": 1787048403443,
        "deleted_at": null,
        "created_at": 1787048403443,
        "schedule_id": "0c90a1ea-a155-45da-9061-205fbdf54a8f",
        "medication_id": "fcf52009-1ae9-46d0-bccf-25ef120d8cdd",
        "profile_id": "6a1af946-74bd-4b19-a210-fdf46e1cf525",
        "planned_at": 1787048403443,
        "status": "pending",
        "action_at": null,
        "snooze_count": 0,
        "snoozed_until": null,
        "dose_amount": 1.0
      }
    ],
    "stock_events": [
      {
        "id": "c3a62154-eb37-4366-87f7-f58344a8c3b6",
        "updated_at": 1787048403443,
        "deleted_at": null,
        "created_at": 1787048403443,
        "medication_id": "fcf52009-1ae9-46d0-bccf-25ef120d8cdd",
        "delta": 30.0,
        "reason": "manual",
        "dose_event_id": null
      }
    ]
  },
  "roles": {
    "6a1af946-74bd-4b19-a210-fdf46e1cf525": "owner"
  }
}
```
[2m2026-08-18T10:20:03.614380Z[0m [[32m[1minfo     [0m] [1mhttp                          [0m [[0m[1m[34mapi.request[0m][0m [36mmethod[0m=[35mGET[0m [36mms[0m=[35m13[0m [36mpath[0m=[35m/v1/sync/pull[0m [36mstatus[0m=[35m200[0m
HTTP Request: GET http://x/v1/sync/pull?cursor=c2VxOjU= "HTTP/1.1 200 OK"

#### GET /v1/sync/pull?cursor=… — ничего нового

```json
{
  "cursor": "c2VxOjU=",
  "has_more": false,
  "changes": {},
  "roles": {
    "6a1af946-74bd-4b19-a210-fdf46e1cf525": "owner"
  }
}
```
[2m2026-08-18T10:20:03.629860Z[0m [[32m[1minfo     [0m] [1mhttp                          [0m [[0m[1m[34mapi.request[0m][0m [36mmethod[0m=[35mPOST[0m [36mms[0m=[35m13[0m [36mpath[0m=[35m/v1/sync/push[0m [36mstatus[0m=[35m200[0m
HTTP Request: POST http://x/v1/sync/push "HTTP/1.1 200 OK"
[2m2026-08-18T10:20:03.644516Z[0m [[32m[1minfo     [0m] [1mhttp                          [0m [[0m[1m[34mapi.request[0m][0m [36mmethod[0m=[35mGET[0m [36mms[0m=[35m13[0m [36mpath[0m=[35m/v1/sync/pull[0m [36mstatus[0m=[35m200[0m
HTTP Request: GET http://x/v1/sync/pull?cursor=c2VxOjU= "HTTP/1.1 200 OK"

#### GET /v1/sync/pull — как выглядит удаление

```json
{
  "cursor": "c2VxOjc=",
  "has_more": false,
  "changes": {
    "medications": [
      {
        "id": "fcf52009-1ae9-46d0-bccf-25ef120d8cdd",
        "updated_at": 1787048463443,
        "deleted_at": 1787048463443,
        "created_at": 1787048403443,
        "profile_id": "6a1af946-74bd-4b19-a210-fdf46e1cf525",
        "name": "Enalapril",
        "notes": null,
        "dosage_text": "10 mg",
        "dose_amount": 1.0,
        "form": "tablet",
        "pack_size": 30.0,
        "refill_threshold_days": 3,
        "is_active": true,
        "photo_key": null
      }
    ]
  },
  "roles": {
    "6a1af946-74bd-4b19-a210-fdf46e1cf525": "owner"
  }
}
```
[2m2026-08-18T10:20:03.658508Z[0m [[32m[1minfo     [0m] [1mhttp                          [0m [[0m[1m[34mapi.request[0m][0m [36mmethod[0m=[35mPOST[0m [36mms[0m=[35m12[0m [36mpath[0m=[35m/v1/pairing/codes[0m [36mstatus[0m=[35m200[0m
HTTP Request: POST http://x/v1/pairing/codes "HTTP/1.1 200 OK"
[2m2026-08-18T10:20:03.686866Z[0m [[32m[1minfo     [0m] [1mhttp                          [0m [[0m[1m[34mapi.request[0m][0m [36mmethod[0m=[35mPOST[0m [36mms[0m=[35m27[0m [36mpath[0m=[35m/v1/pairing/redeem[0m [36mstatus[0m=[35m200[0m
HTTP Request: POST http://x/v1/pairing/redeem "HTTP/1.1 200 OK"
[2m2026-08-18T10:20:03.705500Z[0m [[32m[1minfo     [0m] [1mhttp                          [0m [[0m[1m[34mapi.request[0m][0m [36mmethod[0m=[35mGET[0m [36mms[0m=[35m17[0m [36mpath[0m=[35m/v1/sync/pull[0m [36mstatus[0m=[35m200[0m
HTTP Request: GET http://x/v1/sync/pull "HTTP/1.1 200 OK"

#### GET /v1/sync/pull — ответ наблюдателю (та же выборка)

```json
{
  "cursor": "c2VxOjEw",
  "has_more": false,
  "changes": {
    "profiles": [
      {
        "id": "6a1af946-74bd-4b19-a210-fdf46e1cf525",
        "updated_at": 1787048403443,
        "deleted_at": null,
        "name": "Mum",
        "role": "with_alerts"
      }
    ],
    "medications": [
      {
        "id": "fcf52009-1ae9-46d0-bccf-25ef120d8cdd",
        "updated_at": 1787048463443,
        "deleted_at": 1787048463443,
        "profile_id": "6a1af946-74bd-4b19-a210-fdf46e1cf525",
        "name": "Enalapril",
        "form": "tablet"
      }
    ],
    "dose_events": [
      {
        "id": "cd033526-8a4f-49a8-8937-704d9741f392",
        "updated_at": 1787048403443,
        "deleted_at": null,
        "medication_id": "fcf52009-1ae9-46d0-bccf-25ef120d8cdd",
        "profile_id": "6a1af946-74bd-4b19-a210-fdf46e1cf525",
        "planned_at": 1787048403443,
        "status": "pending",
        "action_at": null
      }
    ]
  },
  "roles": {
    "6a1af946-74bd-4b19-a210-fdf46e1cf525": "with_alerts"
  }
}
```

### 4.5 Фото упаковок — **отложено** (решение владельца 2026-08-11)

`photo_path` в модели устройства — локальный путь, на провод он не идёт (§5.2).
Форма на проводе — `photo_key`, ключ в S3; колонка в схеме уже есть, так что
включение позже миграции не потребует.

Реализация отложена, и вот на каком разборе.

**Опекуну фото не нужны вовсе** — при решении «только статус» (§1.6) они до него
не доходят, как и расписания. **Второму устройству того же аккаунта** — случай
редкий. Остаётся один настоящий сценарий: **смена телефона**, где фото сегодня
теряются, потому что `data_extraction_rules.xml` намеренно исключает
`app_flutter/photos` из облачного бэкапа Android из-за квоты в 25 МБ.

То есть по существу это не функция синхронизации, а функция **резервного
копирования** — пункт 4 scope v1.1 (§0.1), а не часть caregiver-сценария. Ни
один пользовательский путь v1.1 на ней не завязан.

Против неё сейчас работают ещё две вещи. Она требует S3-бакета и, к нему,
IAM-пользователя или роли — то есть **ключа на машине с медданными**, ровно той
работы, которой мы избегали в решениях про бэкапы и KMS. И приоритет: спрос не
подтверждён (§0.3), а функция чинит потерю картинок при смене устройства.

**Когда возьмёмся**, потребуется: узкий IAM-пользователь на один бакет (не
переиспользовать существующий ключ), два эндпоинта —

```
POST /media/upload-url  → { "photo_key": "…", "url": "…", "expires_at": … }
GET  /media/{photo_key} → 302 на подписанную ссылку
```

— и отображение «локальный путь ↔ `photo_key`» на стороне клиента. Наблюдателю
фото не выдаются ни при каком раскладе.

---

## 5. Алерты

Два **разных** сигнала. Сводить их к одному нельзя, и это решение владельца от
2026-08-10.

| Событие | Когда | Что означает |
|---|---|---|
| `dose_missed` | устройство профиля прислало `dose_event` со `status: "missed"`, и прошёл порог | доза **подтверждённо** пропущена |
| `profile_stale` | от устройства профиля нет heartbeat дольше порога | **неизвестно**: связи нет |

Почему не один сигнал: слать тревогу по отсутствию данных — ложные срабатывания,
после третьего их перестают читать. Не слать — настоящий пропуск останется
незамеченным ровно тогда, когда телефон выключен, а это самый тревожный случай.
Разделение честно в обе стороны: наблюдатель видит разницу между «пропущено» и
«мы не знаем».

Следствие для схемы: серверу **не нужно самому считать ожидания по
расписаниям** — о пропусках он узнаёт из `dose_events`.

### Пороги и окна

| Параметр | Значение | Где живёт |
|---|---|---|
| `dose_missed`, задержка перед отправкой | 30 мин | на связке, настраивается |
| `dose_missed`, максимальный возраст дозы | 6 ч | сервер |
| `profile_stale`, порог молчания | **36 ч** | на связке, настраивается |
| `profile_stale`, чьё молчание считается | **только `owner_device_id`** | сервер |
| `profile_stale`, частота | не чаще раза в сутки | сервер, ключ — дата |

Порог молчания был 12 часов. Телефон в Doze за ночь молчит столько при полностью
исправной работе, поэтому опекун получал бы тревогу почти каждое утро, а тревога,
которая обычно ложная, — это тревога, которую перестают читать. 36 часов
переживают ночь, выходные у родни и день без сети.

Максимальный возраст дозы — это нижняя граница детекции. Без неё первый же скан
после выдачи роли `with_alerts` поднял бы алерт на каждую пропущенную дозу за всю
историю профиля: опекун получает стену уведомлений, в которой сегодняшнее
погребено. Одно и то же окно решает, что алерт поздно слать и что его поздно
поднимать.

**`profile_stale` — advisory, и клиент обязан его так и подать.** Он означает
ровно «приложение давно не выходило на связь» и ничего больше. Формулировки вида
«телефон недоступен» или «доза пропущена» — неверны: сервер не отличает
выключенный телефон от лежащего без сети, а пропуск дозы приходит только
сигналом `dose_missed` и только со слов устройства.

Считается молчание **того устройства, которое ставит будильники**
(`profiles.owner_device_id`), а не любого устройства аккаунта владельца. Иначе
второй телефон или планшет, оставшийся в аккаунте, поддерживал бы «всё в
порядке», пока телефон, который реально напоминает, лежит выключенным — то есть
сигнал молчал бы ровно в том случае, ради которого существует. У профиля, где
авторитет не заявлен, сигнала нет вовсе: будильники не ставит никто, и это
другая беда, а не эта.

Сигнал считается по серверному факту «устройство обращалось к API» (любой
авторизованный запрос, в том числе обычный sync), а не по тому, открывал ли
пользователь экран. Пожилой пользователь может неделями подтверждать дозы прямо
из уведомлений и ни разу не зайти в интерфейс — это не молчание.

### Полезная нагрузка push

```json
{ "type": "dose_missed",    "profile_id": "<uuid>", "subject_id": "<uuid дозы>" }
{ "type": "profile_stale",  "profile_id": "<uuid>", "subject_id": "2026-08-13" }
```

Поле называется `subject_id`, а не `dose_event_id`: у `profile_stale` это дата
окна, а не доза. (До 2026-08-13 здесь было написано `dose_event_id` — документ
расходился с сервером, исправлено по факту реализации.)

**Названий лекарств в push нет.** FCM — это Google, и отправлять туда названия
препаратов значило бы выносить данные ст. 9 третьей стороне ради текста
уведомления. Клиент получает ссылку, забирает подробности по TLS и отрисовывает
локально.

### Доставка: at-least-once со схлопыванием

Изменено 2026-08-13 по внешнему ревью. Раньше запись о доставке создавалась **до**
отправки и сразу со `sent_at`: отказ FCM означал навсегда потерянный алерт, о
котором в базе было написано «доставлено». Обоснованием служил размен «лучше
потерять, чем разбудить дважды» — размена нет, потому что FCM схлопывает
сообщения по ключу.

Что гарантируется теперь:

- **Не теряется.** Отправка повторяется с backoff 1 / 5 / 20 / 60 минут, до пяти
  попыток.
- **Не звонит дважды.** Каждое сообщение уходит с `collapse_key` вида
  `"<type>:<profile_id>:<subject_id>"`. Повтор заменяет уведомление, а не
  добавляет второе. **Клиент обязан дополнительно дедуплицировать по
  `(type, profile_id, subject_id)`** — collapse работает на доставке, но
  приложение может получить оба сообщения, если первое уже было обработано.

  `profile_id` в ключе обязателен. Без него у `profile_stale`, где субъект —
  только дата, алерт про одного подопечного молча заменял бы на телефоне алерт
  про другого. Ту же ошибку содержал серверный ключ уникальности: опекун с двумя
  родителями получал один stale-алерт в сутки на двоих. Исправлено миграцией
  `0010`.
- **Не приходит поздно, но и не раньше срока.** TTL: 6 ч для `dose_missed`,
  24 ч для `profile_stale`. Тот же срок проставляется в `android.ttl` — **по
  типу сигнала, не одним числом на всё**. До 2026-08-13 в FCM уходило плоские
  6 часов для обоих: `profile_stale` FCM выбрасывал через шесть часов, сервер
  при этом уже записал `sent` и повторять не собирался, так что выключенный на
  ночь телефон не получал ничего и нигде не оставалось следа потери. Найдено
  ревью. Правило: срок у FCM не может быть короче серверного, иначе потеря
  происходит там, где сервер её не видит. Закреплено
  `test_each_signal_keeps_its_own_ttl_at_fcm`. Просроченный алерт помечается
  `expired` и не отправляется.
- **Провал виден.** Исчерпав попытки, запись переходит в `given_up` с причиной в
  `last_error`. `sent_at` заполняется только реально принятой отправкой.

Мёртвый токен (FCM 400/404) не ретраится: приложение переустановили, и повторы
жгли бы попытки, нужные живому устройству.

### Как вызвать настоящий алерт для E2E

Специального эндпоинта нет и не будет: тестовый путь, отличный от боевого,
проверяет тестовый путь. Боевой вызывается за четыре шага и укладывается в
минуту.

1. Владелец выдаёт код с ролью `with_alerts`, опекун его гасит (§3).
2. Устройство опекуна регистрирует токен: `PUT /devices/{device_id}/push-token`.
   Без него алерт создаётся, но откладывается на 5 минут — «некому сообщить» не
   считается неудачной попыткой.
3. Владелец пушит дозу со `status: "missed"` и `planned_at` в окне
   **от 6 часов до 30 минут назад**. Позже 30 минут — ещё не срок (порог связки).
   Раньше 6 часов — уже поздно (нижняя граница детекции, §5).
4. Ждать до 60 секунд: воркер сканирует раз в минуту.

`profile_stale` вызвать сложнее — нужно, чтобы устройство владельца не обращалось
к API 36 часов. Для проверки маршрутизации проще послать себе `dose_missed`:
форма payload у них одинаковая, различается только `type` и смысл `subject_id`.

Что должно получиться: **ровно одно** уведомление на телефоне опекуна. Повторный
прогон с тем же `dose_event.id` не должен дать второго — ни сервер его не
отправит (ключ уникальности), ни клиент не покажет (дедуп по
`(type, profile_id, subject_id)`).

### Что означает `sent`, и чего оно не означает

**`sent` = FCM принял сообщение. Не «опекун увидел».**

Между этими двумя вещами лежит целиком клиентская сторона, и она уже один раз
оказалась пустой: сообщения приходили, но приложение маршрутизировало все входящие
push в обработчик смены владения, так что `dose_missed` и `profile_stale`
молча пропадали. С сервера это выглядело безупречно — все алерты `sent`, ни
одного `given_up`. Найдено ревью 2026-08-13.

Отсюда правило: серверная статистика доставки **не является** свидетельством
того, что алерты работают. Единственное свидетельство — E2E на живом телефоне.

Три вещи, о которых клиенту надо знать, и все три способны выглядеть как
«сервер не прислал»:

1. **Push может обогнать данные, на которые ссылается.** Воркер сканирует раз в
   минуту и шлёт сразу; наблюдатель тянет `pull` по своему расписанию. Поэтому
   `dose_missed` с `subject_id` вполне может прийти **раньше**, чем доза с этим
   `id` попадёт к наблюдателю.

   Уведомление обязано показываться **не имея ни строки дозы, ни имени
   профиля**. Имя тоже может отсутствовать: опекун, только что погасивший код,
   получит алерт раньше, чем к нему приедет строка профиля. (Правка 2026-08-13:
   здесь было сказано «текст берётся из типа сигнала и имени профиля» — неверно
   ровно в этом случае, и указано ревьюером.)

   Отсюда лестница, а не одна формулировка:

   | Что есть локально | Что показывать |
   |---|---|
   | ничего, кроме `type` | общий текст по типу сигнала |
   | имя профиля | то же, с указанием, о ком речь |
   | доза и лекарство | точная формулировка, уже в приложении |

   Нижняя ступень обязана существовать и обязана быть осмысленной сама по себе.
   Ждать данных — значит не показать ничего, а это и есть тот отказ, ради
   предотвращения которого весь механизм построен.
2. **Payload не несёт текста, и это не оплошность.** Ни названия лекарства, ни
   готовой формулировки — см. выше про ст. 9. Текст уведомления пишет
   приложение, локально, из того, что у него уже есть.
3. **Data-only сообщение не доставляется приложению, которое пользователь
   force-stop'нул**, и фоновые data-сообщения Android квотирует. `priority:
   high` ставится, и это всё, что можно сделать со стороны сервера. Уйти от
   ограничения можно только `notification`-payload, то есть отправив текст про
   лекарства через Google, — на что мы не пойдём. Ограничение принимается
   осознанно и должно быть известно, а не обнаружено.

---

## 6. Схема Postgres

Зеркало device-модели плюс то, чего на устройстве не было. Все таблицы несут
`server_seq bigint` (курсор) и `updated_at`, `deleted_at` из `SyncColumns`.

**Новое, серверное:**

```
accounts             id, google_sub UNIQUE, email_enc, created_at, deleted_at
devices              id, account_id, platform, app_version, push_token,
                     last_seen_at, created_at, revoked_at
refresh_tokens       id, device_id, token_hash, expires_at, rotated_from, revoked_at
profile_memberships  id, profile_id, account_id, role, created_at, revoked_at
pairing_codes        id, profile_id, issued_by, role, code_hash, expires_at,
                     redeemed_at, redeemed_by
```

**Зеркало устройства:** `profiles`, `medications`, `schedules`, `dose_events`,
`stock_events` — поля один в один с `tables.dart`, плюс `profiles.owner_account_id`
и `profiles.owner_device_id` (хозяин напоминаний, §1.4).

**Шифруются** (`bytea`, префикс с версией схемы шифрования):
`accounts.email`, `profiles.name`, `medications.name`, `medications.notes`,
`medications.dosage_text`.

**Не шифруются:** времена, статусы, `server_seq` — иначе сервер не определит
пропуск дозы и алерты перестанут работать. Это осознанная граница, а не
недоделка.

Блоб с версией схемы заложен сразу: переход на клиентское шифрование позже
станет сменой владельца ключа, а не миграцией схемы.

**Бэкапы.** Ночной дамп в S3, шифрованный, с регулярной проверкой
восстановления. Бэкап, который ни разу не разворачивали, бэкапом не является.

---

## 6a. Требования к клиенту

Из работы app-трека над SQL CHECK v6. Вносятся в контракт, потому что это
свойства **провода**, а не их внутренней схемы.

**1. Проверка `isFinite` при десериализации, до записи.** Схема здесь не спасает:
SQLite хранит NaN как NULL, а в `current_stock` и `pack_size` NULL **несёт
смысл** — «учёт выключен», «размер пачки неизвестен». Пришедший NaN молча
выключит функцию, и никакой CHECK этого не отличит. Отбраковка обязана стоять в
разборе, до записи. (`current_stock` мы из синхронизации убрали, но `pack_size`
остаётся, и правило общее.)

**2. Нарушение CHECK не роняет клиент.** Запись, не прошедшую ограничение, клиент
карантинит и продолжает — не падает и не встаёт в цикл повторов. Одна плохая
запись не должна останавливать синхронизацию остальных.

**3. Перечисления и версии.** Сервер не расширяет перечисления без bump'а версии
API; клиент карантинит неизвестные значения.

> Уточнение по механике риска. Сервер сам новых значений не изобретает — он
> зеркалит устройство. Значит опасность не «сервер прислал новое», а **более
> новый клиент прислал значение, которого не знает более старый**, и сервер его
> добросовестно передал. Фильтровать по версии клиента сервер не будет: это
> потребовало бы знать поддерживаемый набор каждой сборки. Поэтому карантин на
> клиенте — не подстраховка, а основной механизм.

---

## 7. Что должен реализовать app-трек

Этого в приложении **нет**, и это предложение, а не решённое:

- **accounts / devices / profile_memberships** как локальные таблицы (§3.2).
- **Флаг reminder-authority** на профиле: материализует ли **это** устройство
  будильники по нему (§1.4). Наблюдаемые профили — всегда нет.
- **Курсор синхронизации**: последний `cursor` от сервера, отдельно для push и
  pull.
- **Отображение `photo_path` ↔ `photo_key`** (§4.5).
- **Постоянный `device.id`**, переживающий перезапуск, но не переустановку.
- **Heartbeat** — иначе `profile_stale` не работает.

Реальный sync не включается, пока не закрыты долги §3.1 (durable alarm outbox,
SQL CHECK) — §7 спеки. `AlarmOutboxEntries` в модели уже есть, работа начата.

---

## 8. Ответ на `05d` — что решено

Ред. 2 учитывает `dosebuddy/docs/05d-api-contract-response.md`.

| Их пункт | Решение |
|---|---|
| §1.1б жёсткие удаления | **Вариант A принят.** Пурж расписания — на `deleted_at`. Правило расширено: свёртка retention для `stock_events` тоже обязана пересекать провод (см. ниже) |
| §2.1 `is_elder_mode` | Подтверждено: не синхронизируется |
| §2.2 `current_stock` | Идём дальше их предложения: **не синхронизируется вовсе**, а не «синхронизируется и пересчитывается» |
| §2.3 операционные таблицы | Запрет внесён явно |
| §2.4 часы устройства | Записано как известное ограничение |
| §3 три требования | Внесены в §6a |
| §1.2 окно 72 часа, §1.3 постраничность | Внесены в §4.3 |

**Одно следствие сверх их ответа, и это порча данных, а не сбой протокола.**
Вариант A закрывает пурж расписания, но не retention. `_retentionCleanup`
сворачивает вычищаемые `stock_events` во **вставку нового** события с суммой и
физически удаляет исходные. Независимая чистка «по возрасту на обеих сторонах»,
как предлагал ответ, привела бы к тому, что baseline уехал бы на сервер, где
исходные события ещё лежат, — сумма журнала удвоилась бы, и свежее устройство
показало бы неверный остаток. Молча: ни ошибки, ни расхождения статусов, просто
неправильное число.

Поэтому свёртка целиком пересекает провод: вставка baseline плюс `deleted_at` на
свёрнутых. Для `dose_events` независимая чистка по возрасту остаётся допустимой —
от их полноты ничего не вычисляется.

---

## 9. Состояние по свёртке retention

Закрыто ред. 3. App-трек взял правку на себя одним изменением: soft delete в
`_purgePending`, свёртка через `deleted_at` с исправленным запросом, вторая
ступень ретеншена и тесты на два последовательных прогона.

Их разбор добавил два следствия, которых не было в моём требовании, и оба
проверены по коду:

**Запрос свёртки не исключает уже удалённые события.** Фильтр — только по
`createdAt`. Сегодня это спит, потому что `stock_events` никто не помечает
удалёнными. Но лечение вводит ровно это, и следующий прогон свернул бы уже
свёрнутое, вставив второй baseline поверх первого. То есть исправление
воспроизвело бы ту же болезнь локально, без всякого сервера.

**Надгробия останавливают чистку доз.** Дозы удаляются только те, на которые не
ссылается ни одно выжившее `stock_event` — внешний ключ, `foreign_keys=ON`.
Запрос ссылок (`doseEventId.isNotNull()`) фильтра по `deleted_at` не имеет, так
что надгробия удерживали бы свои дозы вечно: ретеншен доз тихо перестал бы
работать, таблица росла бы без предела, ошибки не было бы. Отсюда вторая ступень:
через 18 месяцев свёртка с `deleted_at`, ещё через месяц — физическое удаление
надгробий, которое и разблокирует чистку доз. Месяц нужен, чтобы надгробие успело
доехать до сервера.

**Третье следствие — со стороны контракта, см. §4.2:** порождать свёртку вправе
только устройство с reminder-authority. Иначе два своих телефона на одном профиле
свернут журнал независимо и дадут ту же удвоенную сумму без участия сервера.

Порядок: app-трек делает это после SQL CHECK v6 — обе задачи трогают одну
арифметику остатков, и менять их одновременно значит лишиться возможности понять,
что сломалось. Ни то, ни другое не блокирует auth и pairing, которые уже в проде.

## 10. Состояние реализации

| Часть контракта | Состояние |
|---|---|
| Auth (Google Sign-In, токены, удаление аккаунта) | в проде |
| Pairing, членства, устройства | в проде |
| Sync `push` / `pull`, курсор, проекция по роли | в проде |
| Алерты `dose_missed` / `profile_stale` | в проде, at-least-once, FCM подключён |
| Фото упаковок | **отложено**, см. §4.5 |

**Открыто к app-треку** — один блокирующий пункт. Закрытые оставлены записанными,
потому что каждый из них уже был однажды закрыт «в принципе».

| # | Пункт | Состояние |
|---|---|---|
| 1 | Гейт напоминаний читает `role`, а не `profile_memberships` | ✅ закрыт |
| 2 | `dose_events.schedule_id` nullable и без FK; наблюдаемая история отдельно | ✅ закрыт (`observed_doses`) |
| 3 | Маршрут входящих строк по `roles`, а не по форме записи | ✅ закрыт |
| 4 | Удаление локальных данных профиля, пропавшего из `roles` | открыт |
| 5 | **Caregiver-алерты не показываются на телефоне** | **блокер** |
| 6 | Отвергнутая строка (`immutable_parent` / `forbidden_role`) — в карантин, не в «применено» | открыт |
| 7 | Исходящий батч замкнуть по родителям: ребёнок не уходит раньше родителя | открыт |

**№5, найдено ревью 2026-08-13.** Приложение маршрутизирует все входящие push в
обработчик смены владения (`handleOwnershipPush` в foreground,
`ownershipPushBackground` в фоне). Типов `dose_missed` и `profile_stale` в
приложении нет вовсе, локального уведомления нет, дедупа нет. Сервер отправляет
их исправно и помечает `sent` — то есть **со стороны сервера всё выглядит
безупречно**, а телефон опекуна не показывает ничего. Требование v1.1 о
caregiver-алертах не выполняется.

Что нужно на их стороне:

1. Развести маршрутизацию команд владения и caregiver-алертов по полю `type`.
2. Записывать ключ дедупа `(type, profile_id, subject_id)` **до** показа, durable.
3. Показывать локальное уведомление, **не обращаясь ни к локальной строке дозы,
   ни к имени профиля** — push может обогнать и то, и другое, см. §5, «Что
   означает `sent`». Подробности подтягивать позже, в authenticated foreground.
4. Отдельный notification channel для caregiver-алертов — **не тот, что у
   напоминаний о пополнении**. Это разные адресаты и разная срочность, а общий
   канал означает, что отключив одно, пользователь молча отключает другое.
5. E2E: сервер → payload → обработчик → одно уведомление; повтор второго не
   создаёт.

Маршрутизацию входящих строк вести по `roles`, а не по наличию `schedule_id`
(§4.3): у собственной внеплановой дозы этого поля тоже нет.

4. **Удаление локальных данных профиля, исчезнувшего из `roles`** (§4.3).
   Отзыв согласия обязан доходить до устройства опекуна, а не выражаться тем,
   что строки перестали приходить.

6. **Неизменяемый родитель** (§4.2, ред. 16, 17 и 18). Сервер отвергает смену
   родителя у существующей строки — целиком, ни одного поля — у всех шести
   связей, а не только у лекарства: `medications.profile_id`,
   `dose_events.profile_id`, **`dose_events.medication_id`**,
   `schedules.medication_id`, `stock_events.medication_id`,
   `profiles.owner_account_id` (последняя отвечает `forbidden_role`). Клиенту нужно то же самое у себя: отвергнутую версию не
   применять ни одним полем и класть в карантин. Строка, оставшаяся в
   «применено», разойдётся с сервером молча — именно тем способом, от которого
   правило и заводилось. Порядок был согласован обратный обычному: сервер
   выкладывается первым, клиентский guard идёт следом.

7. **Порядок внутри исходящего батча** (§4.2, ред. 19). Триггеры `syncout_*`
   пишут `INSERT OR REPLACE`, поэтому правка родителя даёт ему свежий `seq` и
   уносит его за собственных детей; `pushPending` берёт `ORDER BY seq LIMIT
   batchSize`, и граница батча отправляет ребёнка одного. Замерено app-треком:
   профиль на сервере есть, лекарств у него ноль, журнал пуст, в карантине одна
   строка `direction=out`. Свежий `seq` при `REPLACE` снимать нельзя — на нём
   держится корректность уборки батча, — значит замыкать надо сам батч,
   транзитивно, либо вводить старшинство сущностей вторым ключом сортировки.
   **Ред. 19 не смягчает этот дефект, а меняет его форму, и вторая форма шире.**
   Замер app-трека, постоянный тест `outbox_parent_before_child_test.dart`:
   `batchSize = 25` (их `minBatchSize`, пол после нашего `413`), один профиль,
   29 его лекарств впереди него в очереди. Все 25 строк батча — дети, все
   возвращаются в `retry`, ни одна не снимается с журнала, следующий батч тот же
   самый. Родитель стоит 30-м и не отправляется никогда: не лишний круг, а
   вечный останов отправки. Цена больше самой загрузки — `onDrained`
   вызывается только при `drainedPush && drainedPull`, а `held` не `drained`,
   поэтому профиль остаётся **незаявленным**. Будильник при этом звонит:
   `isReminderAuthority` читает NULL в `owner_device_id` как «наш», и это
   записанный инвариант, а не случайность — незаявленные профили сегодня
   большинство состояния. Цена другая: невозможна передача владения между
   устройствами, у `profile_stale` нет устройства, чьё молчание мерить, и копии
   незаявленного профиля — восстановленный бэкап и оригинал — звонят обе.
   Проверено по их коду с обеих сторон: `retry`-строки остаются в
   журнале, у исходящей очереди нет ни TTL, ни жнеца, и удаляет из неё только
   успешная отправка, финальный отказ и полное стирание данных.

   **Вторая пара, и она уже в проде: лекарство и его дозы.** Профиль
   синхронизирован, лекарство правится — свежий `seq` уносит его за дозы,
   материализованные из него, — а неизвестное лекарство под известным профилем
   это `missing_parent`, то есть повтор, то есть строка остаётся в журнале.
   Замер app-трека: `batchSize` 25, 29 доз впереди лекарства, пять кругов
   `held`, лекарство не уехало. Здесь никакой серверной правки не требуется,
   чтобы получить останов: он идёт сегодня, и запускает его обычная правка
   лекарства, а не окно первой синхронизации. У дозы порядок проверок
   профиль-раньше-лекарства, поэтому на паре профиль/дети дозы уходят
   `forbidden_role` и очередь дренится — там ред. 18 теряет, но не встаёт.

   Зависимость при этом **поведенческая, а не логическая**: кода, который
   ветвится по коду отказа, у клиента нет вовсе (их `grep`: ни одного вхождения
   `forbidden_role`, `missing_parent`, `immutable_parent` вне комментария,
   единственный читатель кладёт его в `reason` карантина). Держит очередь
   движущейся именно то, что окончательный отказ **вычищает строку из журнала**.

   **Порядок выкладки — решение владельца, и счёт двусторонний.** Ред. 18
   теряет строки невосстановимо: финальный отказ вычищает их из журнала в
   исходящий карантин, который не переигрывается, а нового `server_seq`
   отвергнутая строка не получает — клиентский фикс их уже не вернёт. Ред. 19
   не теряет ничего: строки лежат в журнале целыми и уедут в день, когда фикс
   доедет на телефон, — но до того дня отправка стоит, профиль остаётся
   незаявленным, и вместе с ним нет передачи владения и нет базы для
   `profile_stale`. Окно шире, чем «новый пользователь»: вход в другой аккаунт
   зовёт `backfillSyncJournal`, и вся база снова становится неотправленной.

Реальный sync не включается, пока app-трек не закроет долги §3.1 и SQL CHECK v6 —
по §0.5 п.7 сервер идёт впереди клиента, что и произошло.
